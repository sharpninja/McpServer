{
  "name": "memory_add",
  "description": "Add a memory item. Defaults to Workspace scope. Global scope accepts an empty, omitted, or configured default workspace path and stores Scope=Global with no workspace owner. Workspace scope requires a real workspace path.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspacePath": {
        "description": "Workspace path. Optional for Global scope: empty, omitted, or the configured default workspace stores a Global memory with no workspace owner. Required for Workspace scope.",
        "type": ["string", "null"],
        "default": null
      },
      "category": {
        "description": "Memory category",
        "type": "string"
      },
      "text": {
        "description": "Memory text",
        "type": "string"
      },
      "scope": {
        "description": "Memory scope: Global or Workspace (default Workspace)",
        "type": [
          "string",
          "null"
        ],
        "default": null
      },
      "id": {
        "description": "Optional explicit memory id",
        "type": [
          "string",
          "null"
        ],
        "default": null
      },
      "updatedBy": {
        "description": "Optional updater identity",
        "type": [
          "string",
          "null"
        ],
        "default": null
      }
    },
    "required": [
      "category",
      "text"
    ]
  }
}