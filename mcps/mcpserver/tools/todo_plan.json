{
  "name": "todo_plan",
  "description": "Invoke Copilot to create a detailed implementation plan for a TODO item in the workspace.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "description": "TODO item id",
        "type": "string"
      },
      "workspacePath": {
        "description": "Workspace path (required)",
        "type": "string"
      }
    },
    "required": [
      "id",
      "workspacePath"
    ]
  }
}