{
  "name": "todo_implement",
  "description": "Invoke Copilot to implement a TODO item, working through each task in the workspace.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "description": "TODO item id",
        "type": "string"
      },
      "workspacePath": {
        "description": "Workspace path (required)",
        "type": "string"
      }
    },
    "required": [
      "id",
      "workspacePath"
    ]
  }
}