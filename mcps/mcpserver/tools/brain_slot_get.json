{
  "name": "brain_slot_get",
  "description": "Get an external brain-slot definition by slot id.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "slotId": {
        "description": "Slot id",
        "type": "string"
      },
      "workspacePath": {
        "description": "Workspace path (required)",
        "type": "string"
      }
    },
    "required": [
      "slotId",
      "workspacePath"
    ]
  }
}