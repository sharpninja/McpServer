{
  "name": "brain_slot_orchestrate",
  "description": "Run the full four-role Quad-Brain decision loop.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspacePath": {
        "description": "Workspace path (required)",
        "type": "string"
      },
      "input": {
        "description": "Input prompt",
        "type": "string"
      },
      "turnId": {
        "description": "Owning session-log turn id",
        "type": [
          "string",
          "null"
        ],
        "default": null
      },
      "admitCuriosityToGraphRag": {
        "description": "Whether committed Curiosity output should be admitted to GraphRAG/context",
        "type": "boolean",
        "default": false
      },
      "metadataJson": {
        "description": "Optional JSON object of string metadata",
        "type": [
          "string",
          "null"
        ],
        "default": null
      },
      "weightUpdateJson": {
        "description": "Optional JSON QuadBrainWeightUpdateRequest to apply after final commit",
        "type": [
          "string",
          "null"
        ],
        "default": null
      }
    },
    "required": [
      "workspacePath",
      "input"
    ]
  }
}