# Weighted Context Projection — Addendum: Retrospective Linking and Goal Metrics

**Document:** addendum-retrospective-linking-and-goal-metrics-v0.1.md
**Version:** v0.1.12
**Status:** Draft. Codex findings from merged PR #56, the PR #58 pin exception, and the Astra re-reviews through round 6 are incorporated below. Round-6 text for P2-07 is claimed remediation, pending an independent Astra review. It is not an AGREE. Not operator-approved. Proposes a capability beyond the v0.1.x scope of the whitepaper and the proposed implementation; nothing here is approved or scheduled.
**Companion to:** `whitepaper-weighted-context-projection-v0.1.md` (v0.1.15) and `proposed-implementation-weighted-context-projection-v0.1.md` (v0.1.10)
**Code baseline:** `main` @ `e7c43a125e1bb4837b5b9b9d4021ae2b592f931f`
**Date:** 2026-09-20 (revised 2026-09-25, v0.1.12)
**Grounding:** Byrd Development Process v4 (`docs/Development-Process-draft-v4.md`), the plan artifacts in `docs/plans/`, and `scripts/Validate-RequirementsTraceability.ps1`. §6 is written against that process rather than proposing a parallel one: the plan is the goal, and its declared requirements and acceptance criteria are the scope.

**Origin:** Operator design session, 2026-09-20. Five proposals, recorded here in the order raised: a retrospective link index (§2–§4), continuous link revalidation (§5), per-goal scope and progress metrics (§6), per-turn result-with-explanation records (§7), and Hostile Validation as the adjudicator of those records rather than the acting agent (§6.3) — an operator correction to this draft's first version, which had the actor grading itself.

**Cross-reference convention.** Bare `§N` refers to a section of this document. `WP §N` refers to the whitepaper. `IMPL §N` refers to the proposed implementation.

**Scope.** This addendum is deliberately separate. The whitepaper and the proposed implementation are in operator review at v0.1.15 / v0.1.10. Shared rules already live in both places so they cannot drift: pin and introduced-harm precedence (WP §6.2, §7.4), the transition and cooldown contract (WP §6.1), tool-loop budget (WP §6.3), and the authoritative progress series (§6.1, WP §5.1). If the linking design is later folded in, §2, §3, and §6.1 belong in the whitepaper as design, and the rest belongs in the implementation proposal.

**Status of every claim here.** Design argument and mechanism, not measured result. §11 states the non-claims explicitly. Code references were read against the baseline commit; they are a snapshot and could be wrong.

---

## 1. Why this exists: weight-triggered re-admit does not guarantee recovery of joint value

WP Principle 4 makes re-admit first-class: "if a quiet constraint suddenly matters, the scorer must be allowed to pull the full turn back from SessionLog." WP §6 step 7 gives it exactly one trigger — an omitted turn's `weight` rising enough on a later reweight pass.

That trigger is not blocked by the turn's absence from the assembled projection. WP §6 step 1 loads every SessionLog turn for the session before weights are refreshed, and WP §5.2's v2 judge is given the goal, the recent projection, and candidate-turn stubs. An omitted turn is absent from the model-facing assembly and present, as a stub, in the reweighting input. The scorer can compare that stub with the current failure and raise the turn's weight. An earlier draft of this section claimed re-admit cannot fire because omission hides the turn from the scorer. That claim contradicts the design this addendum extends, and it is retracted.

What remains is a representation limit, not an invisibility limit.

The scorer sees `summaryText`, not the sealed payload (WP §4.2, WP §6 step 5). A stub that dropped the quiet fact (an operator constraint, the exact error text, an environment condition (WP §5.1)) cannot suggest the relevance of that fact, and the v2 judge has no other view of an omitted turn. Locally obvious relevance that survives in the stub (a repeated identifier, the same file) can still raise weight. Four different holes are easy to collapse into one, and they are not the same hole (§2):

- **Missing input.** A fact that is absent from the stub is not available to infer. The judge cannot use a payload it was not given.
- **Contextual inference.** WP §5.2 gives the v2 judge the goal, the recent projection, and several candidate stubs on one call. Independent numeric outputs do not forbid that judge from noticing a relation among those inputs.
- **Persistent relation storage.** A scalar written back to one turn does not keep that noticed relation for a later call. The link index is the stored relation.
- **Dependency-aware packing.** A stored relation still does not put both endpoints in the assembly. That is joint admission (§3.1).

Re-admit can fire when the stub still carries the signal. It can also fire when the v2 judge sees several stubs and raises a weight because it noticed a relation (§2). What re-admit does not guarantee is recovery of joint value: a scalar does not persist the relation, and a higher weight does not pack the other turn. A stub that dropped the fact is a missing input, so that path has nothing to infer from. WP §10 risk 2 ("bad scorer drops quiet constraints") names the symptom; its stated mitigation is a constraint detector at write time, which helps only for constraints recognizable as constraints when written, and only if that detector's output is retained in the stub (§7.3).

The link index below is the proposal for the part a scalar does not guarantee. It does not replace the scorer's existing path from stub to weight. The per-turn scalar does not by itself persist a pair or co-admit both turns (§2). The quiet-constraint case, a fact recorded in passing with no shared criterion id, is still a candidate. Exact referent match is not enough when the failure does not quote the constraint. §5.4 requires both a referent match, when a checkable string is shared, and a `constraint-fact` scan that proposes marked constraint turns even when the failure never quotes them. A walk of the requirement graph alone does not recover that case.

For provenance: the reinforcement idea this whole design rests on is stated in BDPv4's monitoring list, where a steering message that re-surfaces workspace instructions after compaction "reinforces the weight applied to those requirements, which over time help the compaction algorithm to retain such instructions" (`docs/Development-Process-draft-v4.md`, Implementation). Weighted projection is a mechanization of that observation, and §6 is the part that supplies the progress measurement the observation assumes.

---

## 2. What a stored scalar does not guarantee

The deeper issue is what `weight` can represent.

Consider a session where turn 4 records an environment constraint in passing and turn 60 hits a failure the constraint explains. Scored individually against current work, turn 4 is unremarkable — it mentions no current identifier, it is old, it reads as background. Turn 60 is obviously relevant. The information that resolves the situation is not in either turn; it is in the *pair*.

WP §4.2 stores one scalar `weight` per turn. WP §5.2's v2 judge is not an isolated reading of one turn: its prompt contains the goal, the recent projection, and multiple candidate stubs. The number it writes is still one scalar per turn, and those scalars are not a normalized distribution (WP §4.2). Independent outputs mean one turn's score is not forced to fall because another turn's score rose. They do not mean the judge is unable to notice that two stubs in that same prompt bear on each other.

What the scalar does not do is the rest of the job:

- It does not fill in a fact the stub dropped. That is a missing input, not a property of the output type.
- It does not persist the noticed pair. The next call does not inherit an unnamed relation from the previous scalar.
- It does not pack both turns together. Co-presence is §3.1, not a side effect of either score.

The link index is the persistent relation. The context-conditioned scalar, run on the same stubs without that relation, is the baseline §9's without-admission replay measures. Beating that baseline is an empirical question. It is not entailed by the output being a scalar.

This is a generalization of the pattern already accepted elsewhere in the design: supersession and `tokenEstimate` both turned out to be *relations* or *derived functions* rather than intrinsic properties of a turn (IMPL §3.4, §5.4). Links are the same move applied to relevance.

---

## 3. Retrospective link index

**Proposal.** A service that periodically examines sealed turns and records typed, directed, weighted edges between turns whose connection is discoverable only in hindsight. The projector may then admit one endpoint because the other is load-bearing, rather than only because its own score rose, and only in the direction that edge type allows (§3.1). A link is not a license to pull either end into the assembly.

**Why sealing makes this tractable.** IMPL §5 seals a turn's payload at terminal status. An index over immutable content is stable, cacheable, and safely recomputable: re-running the indexer over the same sealed turns cannot produce different inputs. Without the seal, every index entry would be suspect the moment a turn was edited.

**Where links live.** Edges are derived data and belong in the mutable projection layer alongside `weight` and `projectionState`, never in the seal. This follows the resolution already reached twice: IMPL §5.4 derives supersession rather than writing it into the immutable row, and IMPL §3.4 derives `tokenEstimate` per renderer rather than freezing one value. A link is a hypothesis about content, not part of the content.

**Link types must be task-progress relations, not similarity.** WP Principle 2 rejects raw self-information, recency alone, and "interestingness" as weighting bases. A `similar_to` edge reintroduces all three under a new name, and topical similarity is both the cheapest thing to compute and the wrong thing to act on — it will happily link every turn that mentions the same file. Admissible edge types are ones that assert something about work:

| Type | Asserts |
|---|---|
| `constrains` | A commitment recorded in the source turn limits what the target turn may do |
| `invalidates` | The source turn makes a decision or result in the target turn no longer valid |
| `explains` | The source turn supplies the cause of an outcome observed in the target turn |
| `shares_root_cause` | Two symptoms trace to one underlying condition |
| `depends_on` | The target's result is contingent on the source's result holding |

The list is not closed. A new type is admissible only when traversing it changes what work should happen next, and only after §3.1 has a row for which endpoint may be admitted. "These are about the same topic" fails that test. There is no default admission direction for a type that lacks a row.

**Every edge carries its evidence.** An edge records the turns it connects, its type, a confidence value, and a reference to what justifies it — which acceptance criterion, which identifier, which recorded outcome, which sealed facts. An edge whose justification cannot be inspected cannot be audited when it turns out to be wrong, and §4 argues wrong edges are the primary hazard. Shared membership in a criterion is a candidate (§5.4), not this justification, and not a type.

### 3.1 Direction-specific admission

Directed storage records which turn is the source and which is the target. It does not say which endpoint may re-admit the other. A blanket rule — if either end is load-bearing, admit the other — injects false context even when the edge is correct. When a load-bearing source `invalidates` an older target, admitting an unpinned target's full payload injects the obsolete result the edge identifies. When a source `constrains` a target, the direction that matters is the opposite: the constraint is what the projection was missing.

Admission is defined per type. An edge whose type is not in this table is not traversable for admission until its row exists. There is no default direction.

| Type | Consult the edge when | Admit | Do not admit by this edge |
|---|---|---|---|
| `invalidates` | The target is in the assembly or is a plausible retry, or the source is load-bearing and names a result the actor might follow | The source, as the invalidating decision, expanded or stubbed. If an unpinned target is already present, force it to a marked stub: the obsolete result, and that this edge invalidates it. If the target carries an operator, system, or acceptance pin, leave that target expanded and attach the same invalidation marker separately | An unpinned target's full payload. That payload is the obsolete result |
| `constrains` | The target is load-bearing | The source, the commitment that limits what the target may do | The target. The constraint does not make the constrained work current |
| `explains` | The target, the observed outcome, is load-bearing | The source, the recorded cause | The target. It is already why the edge was consulted |
| `depends_on` | The target is load-bearing | The source, the precondition the target's result is contingent on | The target. Later contingent work is not pulled in because its precondition is in view |
| `shares_root_cause` | Either endpoint is load-bearing | The other endpoint. Storage direction is arbitrary; admission is symmetric, and neither end is obsolete by this type alone | Nothing is retired by this type. A separate `invalidates` edge is required before an unpinned payload is forced to a stub |

**Pins stay expanded.** The precedence is WP §6.2, which this section restates and does not replace. An operator, system, or acceptance pin stays `expanded` until unpinned, including when this edge is `invalidates` and including when the turn later draws an introduced-harm verdict (WP §6.2, §7.4). The invalidation marker (the result is obsolete, and which edge says so) is attached beside the expanded pin and charged against `B`. If the pin plus the marker exceeds `B_turns`, WP §6 fails closed. An unpinned obsolete target that is already present is forced to the marked stub in the table. The same pin rule covers every instruction in this section that forces a payload to a stub, including `shares_root_cause`. These forces are mandatory overrides in WP §6.1: they do not wait on weight hysteresis or re-admit cooldown.

**Final-assembly dependency invariant.** Co-presence is a property of the assembly that will be sent, not only of the edge's admit bit. An endpoint that is in that assembly for any independent reason (an active pin, a discretionary score that already cleared WP §6.1, the live result, or another edge already in the closure) still requires the counterpart this table names. "The edge admits neither" applies only when the target is in the assembly solely because of this edge. It does not remove a pin, and it does not leave a pinned or otherwise retained target in place while its required context is silently absent.

Traversal uses one closure per generation:

- **Depth.** Provisional **`Dep = 2`**. A newly required source may itself require a source, and that walk stops at depth 2 from the originally retained endpoint. A deeper edge is not admitted as a payload. Depth truncation is not an audit-only note. The retained endpoint that required the truncated source is rendered by the same rule as no-fit: `invalidates` fails closed, and every other type attaches a dependency-gap marker on that retained endpoint naming the missing source, the edge id, the type, and that the absence is `depth-capped`. An endpoint that entered the closure only as someone else's source is independently retained for its own further dependency. Sending it with the further source silently absent is non-conforming.
- **Cycles.** If the next endpoint is already in the closure, record `cycle` and stop that branch. Do not re-expand it.
- **Dedup and cap.** A turn named by several edges is charged once against the link sub-cap. A full source payload is one admission charge. A dependency-gap marker is a marker charge, not an admission charge.
- **No fit, target present only by this edge.** For `explains`, `constrains`, `depends_on`, and `shares_root_cause`, admit neither endpoint. For `invalidates`, fail closed under WP §6.2 and WP §6.3. Do not drop the pin, do not omit the marker, and do not admit the source while dropping the target's required form.
- **No fit, target independently retained.** Keep the target. For `invalidates`, fail closed: there is no degraded form that omits the source marker or the pin. For the other types, attach a **dependency-gap marker** on the target: edge id, source turn id, edge type, and the statement that required context is absent and the target must not be treated as unconstrained. The edge state is `degraded`, not admitted. The marker is mandatory and spill-protected (WP §6.3). Sending the target with neither the source nor that marker is non-conforming.

**Depth-three acceptance case.** Pinned T `depends_on` S1, S1 `depends_on` S2, S2 `depends_on` S3. The token budget can hold T, S1, S2, and S3. `Dep = 2`, so S3 is not admitted as a payload. S2 is in the assembly because the walk admitted it. The sent prompt carries a dependency-gap marker on S2 naming S3. Depth is the only reason S3's payload is absent. If the S2-to-S3 edge is `invalidates`, the call fails closed instead of sending S2 without that source.

Every link-admitted turn is marked as such (§4). The link sub-cap still applies. Tombstoned endpoints are ineligible on both sides (§8).

---

## 4. Why false links are worse than false weights

A weighting error has two forms. It can omit something useful. It can also admit something misleading: an erroneously high weight can expand an obsolete or harmful turn and displace current work. Both are weighting errors. The budget invariant still holds either way, and an omitted turn is still recoverable per WP §6. That recoverability is not a claim that a false weight leaves the projection honest, or that false weights are safe.

A link error injects content carrying an implied causal claim: this material bears on your current situation. That causal endorsement is a stronger risk than a bare weight, and it is a hypothesis to measure against the scalar baseline (§9's without-admission replay). It is not a measured result, and it is not a reason to treat false weights as categorically harmless.

Three consequences:

1. **False admission is the risk under test.** A wrong lead is an injected claim, so this design treats a false admission as more costly than a missing edge until measurement says otherwise. That cost ordering is a testable risk hypothesis, not a universal cutoff and not a measured result. The sentence "a link index at 60% precision is worse than no index" is withdrawn as a fact. Whether a measured precision is too low to admit edges is the predeclared gate in §9 (admission precision on the labeled shadow sample). Until that gate passes, edges MAY be recorded and revalidated, and MUST NOT admit turns into the model-facing projection. A shadow log of would-be admissions (§9) is a record for the gate. It is not admission. Prefer few, strong edges while the hypothesis is open.
2. **Link-admitted turns need a sub-cap inside `budgetTokens`.** Otherwise a burst of spurious edges can crowd out current unpinned work and force a fail-closed budget error. The sub-cap MUST NOT demote an active pin. Pins stay expanded under WP §6.2. If pins plus their markers plus the link sub-cap exceed `B`, the invocation fails closed (WP §6.3). The hard budget remains the outer bound; link admissions get a fraction of what remains after pins.
3. **Marking matters.** A turn admitted by link should be identifiable as such in the assembly, so a reader — human or model — can discount it. Silent injection removes the ability to tell a retrieved lead from established context.

### 4.1 Prior art: the adversarial link graph

Web search solved a structurally identical problem — a link graph whose edges are cheap to fabricate and profitable to fabricate — and the public record is worth reading before designing an indexer, because it does not vindicate the §4 position as stated.

**Links are weighted, not counted.** Google's "reasonable surfer" work models a user who follows some links more often than others, weighting each link by the probability it is actually clicked given position, prominence and context, rather than treating every link on a page as an equal vote ([US 8,117,209 B1](https://patents.google.com/patent/US8117209B1/en)). This is the nearest public analogue to typed, weighted edges over uniform ones. It is not a relation taxonomy.

**Bad edges are neutralized, not punished, and the credit is not recoverable.** Google describes using its spam system "to neutralize the impact of unnatural links on search results," with ranking changing "as spammy links are neutralized and any credit passed by these unnatural links are lost" ([Google Search Central, December 2022](https://developers.google.com/search/blog/2022/12/december-22-link-spam-update)), and states that once "our systems remove the effects spammy links may have... Any potential ranking benefits generated by those links cannot be regained" ([Google Search Central documentation](https://developers.google.com/search/docs/appearance/spam-updates)). That is §5.5's retraction lifecycle: withdraw the edge's contribution, keep the record.

**Edge annotations are hints, not directives — explicitly so information is not lost.** Google treats `sponsored`, `ugc` and `nofollow` "as hints about which links to consider or exclude within Search," reasoning that "by shifting to a hint model, we no longer lose this important information, while still allowing site owners to indicate that some links shouldn't be given the weight of a first-party endorsement" ([Google Search Central, 2019](https://developers.google.com/search/blog/2019/09/evolving-nofollow-new-ways-to-identify)). An edge marked untrustworthy is retained and downweighted rather than deleted — the same choice §5.5 makes.

**Trust is propagated from a human-evaluated seed set, as a counter-bias rather than a score.** TrustRank selects "a small set of seed pages to be evaluated by an expert," propagates trust along links, and is offered as "a counter-bias to be applied when results are ranked, in order to discount possible boosts achieved by spam," noting that plain PageRank "does not incorporate any knowledge about the quality of a site, nor does it explicitly penalize badness" ([Gyöngyi, Garcia-Molina and Pedersen, VLDB 2004](https://www.vldb.org/conf/2004/RS15P3.PDF)). This is the one result here that transfers cleanly: §6.4's passing tests and §6.3's HV verdicts are an expert-evaluated seed set, and edge confidence should decay with distance from a verified referent rather than being asserted per edge.

**The uncomfortable part.** The load-bearing signal in web search turned out not to be the link graph at all. A Google engineer's internal presentation, entered as a trial exhibit, states: "We do not understand documents. We fake it... Beyond some basic stuff, we hardly look at documents. We look at people. If a document gets a positive reaction, we figure it is good" ([UPX0203, U.S. v. Google](https://www.justice.gov/d9/2023-11/417516.pdf)). Google staff have since said links are not among the top three ranking signals and that "we need very few links to rank pages" ([Search Engine Land](https://searchengineland.com/links-google-search-ranking-factor-gary-illyes-432422)).

So the honest reading of the prior art is that the declared graph was never made trustworthy — it was **demoted in favour of an independent signal the ranking system did not author**: recorded reactions from millions of parties with no stake in any particular document's rank. A single agent session has no such population. §6's insistence that progress be read from acceptance-criteria state, and §6.3's insistence that the verdict come from a party that did not do the work, are the only available substitutes, and they are far weaker than a billion independent observations.

Two further disanalogies constrain how much of this transfers:

- **Opacity is not available.** Web search can partly defend a signal by not publishing it, and even that eroded — the 2024 Content Warehouse documents contradicted public statements about whether site-level authority and click signals existed. Here the actor is the same class of model, reading the same repository, including this document. Any anti-gaming property that depends on the actor not knowing the rule is void by construction.
- **The adversary is inside the system.** Web spam is third parties with a profit motive, and the remedy is demotion of the document. Here the misaligned party is the actor itself, and the closest available remedy is the one BDPv4 already prescribes for a session whose trust has broken: end it and start over (§6.4).

---

## 5. Continuous revalidation

**Proposal.** Model reconsideration is the capped schedule in §5.2 and §5.4. It is not a walk of every live edge after every turn. Tombstone and seal checks for edges in the assembly that will be sent are mandatory at the send fence (WP §6.1, §8). An edge that fails either is ineligible. Changed evidence forces the same ineligibility even when seal sequences, weights, projection states, and goal attribution are unchanged (§5.2). Stale edges and never-judged proposals stay on resumable cursors; the skip rule MUST NOT drop those cursors. A model is asked for the ordered subset in §5.2, subject to the per-generation cap, and each generation serves at least one oldest unjudged candidate when any exist (§5.4). A discretionary admission flip has to clear the §5.3 hysteresis band and the WP §6.1 cooldown. A mandatory invalidation (reseal, tombstone, or changed evidence) removes eligibility immediately and does not wait on that band. Links are per-generation hypotheses. Whether they are precise enough to admit turns is measured with independent labels (§9), not by how often edges are retracted.

Three constraints make the difference between this working and this entrenching errors.

### 5.1 Evaluation must be blind to the projection it influenced

If a false edge admits turn 4, and the next evaluation judges that edge while turn 4 is present in the assembled context, the evaluator sees a coherent narrative and confirms the edge. The edge has become its own evidence. Each generation then raises confidence in a false link, and revalidation — the mechanism introduced to remove false links — entrenches them instead.

**Requirement:** edges are judged against sealed payloads and recorded outcomes directly, out of band, never against the projection they contributed to. The convenient implementation is to ask the model about the context it already has; that implementation has the failure built in.

### 5.2 Evaluate the decision boundary, not the graph

Re-judging every edge after every turn is `O(E)` model judgments per turn. With candidate generation producing even a sparse `k` edges per turn, `E` grows with session length, so cost per turn grows and total session cost is superlinear — while competing with the live turn the index exists to help. WP §10 risk 6 already flags per-turn re-score cost as a live concern; this multiplies it.

Model calls are spent on edges that could change a decision this generation, plus edges the skip rule still requires: changed evidence and stale edges (§5.2 items 3 and 6). An edge between two turns that are both currently omitted, with confidence well away from the admission threshold, with unchanged evidence, and not yet stale, cannot alter the projection no matter how it is judged. That edge is the one the skip rule may pass over.

The requirement graph also bounds which pairs are eligible in the first place — see §5.4, which is the cheaper half of this problem. It generates candidates. It does not supply the causal type.

**Judgments are bound to seal sequences.** Each confirmed or retracted judgment records the seal sequence of both endpoints it was judged against. IMPL §5.4 derives the current seal as the row with the highest sequence. A later judgment against a different pair of sequences is a new transition (§5.5), not an edit of the old one. An edge is eligible for admission only while both endpoints' current sequences match the sequences on its latest confirming judgment.

**Reseal invalidates.** IMPL §5.1 permits a post-terminal mutation - `ReplaceTurnAsync`, section replace, item delete, title set, or a dialog append on an already terminal turn - to produce a new seal without changing `weight`, `projectionState`, or goal attribution. Those three are not a sufficient change detector. If either endpoint's current seal sequence differs from the sequence the edge was last judged against, the prior judgment is void and the edge is ineligible for admission until it is re-judged against the new payloads. The superseded payload is not evidence for the current edge. A reseal is an invalidation trigger ahead of any model call. It is ordering item 2. Changed evidence is ordering item 3 and is the same class of invalidation: the prior judgment is void even when weight, projection state, goal attribution, and seal sequence are unchanged.

Ordering, highest priority first. The per-generation judgment cap spends this list in order. Items that do not fit are deferred and recorded. A deferred edge whose prior judgment was voided (items 2 and 3) stays ineligible for admission until a later generation judges it.

1. Edges currently admitting a turn.
2. Edges whose endpoint seal sequence changed since the last judgment, including reseal after post-terminal mutation (IMPL §5.1, §5.4).
3. Edges whose cited evidence changed since the last judgment, even if seals, weights, projection states, and goal attribution did not. Cited evidence is any checkable referent the edge records (§3): acceptance-criterion state, bound-test pass or fail, an appended or superseded outcome record, an appended or superseded explanation, or a change in turn-to-criterion attribution. A superseded payload or a superseded explanation is not evidence for the current edge.
4. Edges with an endpoint whose `weight`, `projectionState`, or goal attribution moved this generation.
5. Edges near the admission threshold in either direction.
6. Stale edges: generations since last evaluation are at least `S`. Provisional **`S = 10`**. Operator-adjustable, and MUST be fixed before a revalidation run can pass or fail.

**Skip rule.** No model call is made for an edge only when all of the following hold: both endpoints are non-deleted; both seal sequences still match the latest judgment; no cited evidence changed; neither endpoint's weight, projection state, or goal attribution moved; the edge is not near the admission threshold; and the edge is not stale under `S`. Changed evidence forces a judgment. Staleness forces a judgment. Neither is skipped because weight and seal happened to be stable. When the cap is full they are deferred, recorded, and, if the prior judgment was voided, ineligible for admission.

**Judgment slots are reserved, so stale edges cannot be starved.** `J` (provisional **8**) is the model-call cap. It is split, and a configuration with `J_stale < 1` or `J < J_void + J_stale` is non-conforming:

- `J_void` (provisional **4**) is spent on ordering items 2 and 3 (voided judgments). Unused `J_void` slots MAY be used by stale edges, then by first judgments, then by items 1, 4, and 5.
- `J_stale` (provisional **2**) is reserved for ordering item 6. Whenever any stale edge exists, these slots MUST be spent on the oldest stale edges. Items 1, 4, and 5 MUST NOT consume `J_stale`.
- `J_first` (provisional **1**) is reserved for edges and proposals that have never been judged. Items 1, 4, and 5 MUST NOT consume `J_first` while any never-judged candidate remains. A configuration with `J < J_void + J_stale + J_first` is non-conforming.
- The remainder (`J - J_void - J_stale - J_first`, provisional **1**) is for items 1, 4, and 5.

A generation that spends a `J_stale` slot on a non-stale edge while a stale edge remains is non-conforming. Deferral of a voided edge still leaves that edge ineligible for admission.

**Starvation promotion.** If generations since an edge's last judgment reach `S + S_starve` (provisional **`S_starve = 10`**), that edge is judged before items 4 and 5, from `J_stale` and then from unused `J_void`. Promotion does not void the edge. It stops a standing diet of load-bearing and near-threshold edges from postponing item 6 forever.

**Contradictory evidence outside the citation set.** Reserved stale slots schedule review. They do not define what a review must consider. A confirming judgment records the citation set it used and a `claimFamily`: the approach name or result name the explanation asserts. A later turn can dispute that family without resealing either endpoint, without superseding their outcome records, and without changing a criterion id already in the citation set.

On append, a turn that records a disproof, a failed reproduction, or an HV label that a named approach or result does not hold MUST be marked `contradicts` and MUST name the `claimFamily` it disputes. That name need not be a string already stored in any edge's citation set. The append bumps `evidenceWatermark(claimFamily)` by one. The handler does not walk every dependent (§5.4). Every effective edge with that `claimFamily` is **suspended** when its stored `evidenceVersion` is below the watermark, including edges whose citation set does not contain the new turn's referents.

**Eligibility while adjudication is pending.** A suspended edge's admission bit is clear. It MUST NOT admit, MUST NOT satisfy joint admission (§3.1), and MUST NOT influence packing or a sent prompt. The prior confirming record stays in the audit log. Suspension is not a claim that the old judgment was edited. If the judgment cap defers the re-judgment, the edge stays suspended. Re-judgment reads both sealed endpoints and the contradicting turn. A confirming re-judgment may set the admission bit again under §5.3. Until that record exists, the edge is withdrawn from influence.

**Acceptance trace (third turn, no endpoint edit).** Turns A and B are sealed. Edge E is a confirmed `explains` from A to B. Its citation set is facts inside A and B, and its `claimFamily` is the approach those facts assert. E is admitting A beside B. Turn C is appended. C disproves that approach, does not modify A or B, and does not cite a criterion already in E's citation set. C names E's `claimFamily`. The next generation whose snapshot includes C suspends E before packing. The next send after C is durable MUST NOT carry E's admission. If the cap defers the model judgment, E is still absent from that send. A later judgment that retracts E appends a retraction. A later judgment that confirms E against C may admit again. Neither judgment is required for the withdrawal that precedes the send.

Model-call tokens are bounded by `J * T_j`. The cheap eligibility walk is not bounded by `J`. Its bound is §5.4. Deferred candidates share the backlog cap and the overflow cursor in §5.4.

### 5.3 Edge churn, and why the plan bounds it

Thrashing (oscillating scores, churning projection, dead prompt cache) reappears here because a single edge flipping can swing several turns at once. Discretionary admission flips use the band and the clock in this section. Mandatory invalidation (reseal, tombstone, changed evidence, contradictory evidence under §5.2, introduced-harm on an endpoint) MUST be applied during cooldown. Cooldown blocks only discretionary flips. Edge flip rate is a metric in its own right (§9). Active-context token replacement (WP §9.1) is the metric that shows whether those flips replaced the live tail. A linking run whose thresholds or clock are still the word "provisional" and have not been fixed for that run cannot pass or fail; the numbers below are the contract until an operator replaces them in the run record.

**Edge confidence band.** Provisional, and fixed for a run before that run executes:

- Discretionary admit requires confidence `≥ T_admit`. **`T_admit = 0.80`**.
- Discretionary withdraw of a confirmation requires confidence `≤ T_withdraw`. **`T_withdraw = 0.60`**.
- Confidence strictly between those values causes no discretionary flip.
- Scheduling item 5 ("near the admission threshold") means within 0.05 of either threshold.

**Edge cooldown clock.** `C = 2` is the same constant as WP §6.1, counted in `projectionGeneration`, not wall time. The clock starts when a discretionary admit or a discretionary withdraw changes the admission bit, at that generation. It does not start on forced invalidation. It does not reset because weight moved. Forced invalidation clears the admission bit immediately (§5.2). A later judgment that confirms the same edge again is a mandatory reconfirmation: it MAY set the admission bit without waiting out `C`, and that reconfirmation generation starts a new `C` so the next discretionary withdraw cannot oscillate on the following generation.

**Turn `Δweight` baseline.** WP §6.1 compares `weight` with `weightAtLastStateTransition`, the weight stored when `projectionState` last changed, not with the previous generation's weight. The field updates on every discretionary flip and on every mandatory state change. Repeated steps of 0.04 do not flip while the sum from that baseline stays below 0.05.

**Acceptance traces.** Gradual drift: state last changed at weight 0.50. The next generation is 0.54. Absolute delta from the baseline is 0.04, so no discretionary flip. The generation after that is 0.58. Delta from the baseline is 0.08, so a discretionary flip is allowed if cooldown allows it. Comparing only with the previous generation would have seen 0.04 and would be non-conforming. Forced invalidation: an edge inside cooldown is suspended by §5.2 on the generation the contradicting turn becomes visible, before the next send. A later confirming judgment re-admits without waiting for the old `C` to expire, and starts a new `C`. Capacity that appears because some other turn shrank still does not expand an omitted or summarized turn (WP §6 step 5).

**Hysteresis and cooldown stay primary until turn density is actually bounded.** A BDPv4 phase has a declared, finite acceptance-criteria set (§6.4), and those criteria are pinned by construction under WP §4.2's **acceptance** pin class. That bounds which criteria are in scope. It does not bound the volatile turn set or the edge set. One criterion can accumulate arbitrarily many retry turns, and the edges among them can swap those turns through the residual budget while the criterion itself stays pinned. Pinning the criterion does not pin the turns. v0.1.4 treated the finite set as the first line of defence and hysteresis as a refinement over a bounded residual. That overstated what the anchor bounds. Until a per-criterion density cap is defined - what it counts, the numeric limit, and what the projector does at the cap - hysteresis and cooldown are the primary defence against projection and cache instability. The criteria set remains a scope constraint on which pairs §5.4 may propose. Hysteresis and cooldown are the dampers for discretionary flips. Mandatory overrides in WP §6.1 still apply during cooldown.

Criteria themselves still move slowly, which is a useful property and not a churn bound on turns. BDPv4 expects requirement refinement every iteration, through approved diffs to plan artifacts at human cadence, not per turn. That rate applies to the criterion list. It does not apply to the turns attributed to one criterion.

Three things this does not cover, each of which the dampers still have to hold:

- **Phase boundaries are not thrash.** When a phase or slice completes, the relevant criteria set turns over wholesale and the projection legitimately changes with it. That is a scheduled flush — one predictable cache loss at a known point, which is cheaper than continuous churn — and the §9 flip-rate metric must be conditioned on phase boundaries or it will alarm loudest exactly when the system is behaving correctly.
- **Unattributed turns stay unbounded.** Turns that bear on no acceptance criterion — exploration, environment fights, debugging that led nowhere — fall outside the criteria set, and churn among them is bounded only by hysteresis and cooldown.
- **Bounded criteria do not bound turns per criterion.** A loop that retries the same criterion twenty times produces twenty turns all legitimately attributed to it, and the edges among them are dense, mutually confirming, and nearly worthless. This is why the density cap has to exist before the plan can be described as a churn bound. A criterion accumulating turns without a progress change is the §10 open question 11 deadlock signal rather than a linking opportunity. Until the cap is specified, open question 13 records it as unset, and §5.3 does not assume it.

### 5.4 Candidate generation is a traversal of the requirement graph

The hardest engineering problem in §2–§4 is which pairs to consider at all, and a plan-anchored design answers it without a vector index. Acceptance criteria carry stable identifiers that bind to requirements, test ids, named test methods, and slices (§6.1), and requirement families already have a human-authored relation structure recorded in the traceability mapping and matrix (§6.4). Turns inherit those identifiers by attribution. So candidates are turns reachable through shared or related criteria, not turns that resemble each other.

Three consequences, in descending order of how much they matter:

- **No embedding dependency. Quadratic all-pairs is withdrawn.** §8 records that the only embedding column at the code baseline is on `Chunks`, so an index over turns would have to be built. Traversing identifiers avoids that index for the first cut. It does not make pairing sub-quadratic: the turns on one criterion can still be paired with each other. The bound is the per-generation cap below, not the shape of the graph.
- **Shared criteria generate candidates, not edge types.** "Both turns bear on `AC-FR-MCP-MEMORY-010-02`" is common membership. It does not establish `constrains`, `invalidates`, `explains`, `depends_on`, or `shares_root_cause`, and it does not establish direction. Treating membership as one of those types would be the `similar_to` relation §3 rejects, under a checkable name. The requirement artifact may propose the pair. The causal type and the direction still require evidence in the sealed payloads or the outcome records, or an explicit judgment against that evidence (§3, §3.1, §5.1). The checkable referent §7.1 demands is the evidence for the typed edge, not the shared identifier by itself. The cheap identifier removes the temptation to use similarity as the only signal; it does not remove the judgment.
- **Candidate pairing is narrower than topical similarity; edge precision is not free.** §4 states a hypothesis that false admissions are costly, and §9 turns that hypothesis into a pass/fail gate. Criteria-derived pairs are wrong mainly when attribution is wrong, which is a narrower and more auditable failure than topical drift. A correct pair with an invented type or the wrong direction is still a false lead (§3.1). Proposal precision and admission precision are measured separately with independent labels (§9). Neither is assumed from the candidate generator, and neither is the retraction rate.

**Bounding by phase would reintroduce the hole this addendum exists to open.** The valuable links are frequently long-range — §1's omitted constraint and §2's joint-value pair typically sit in different phases. Time is not the bound. The bound is the plan structure that is actually recorded, which at the baseline supports three traversals:

- The same acceptance criterion across any phase, where a catalog binds that criterion id.
- Sibling criteria under one requirement, where the catalog or the requirement document groups them.
- Co-membership of an FR with its primary TRs and `TEST-*` ids, from `docs/Project/TR-per-FR-Mapping.md`.

**Exact referent match is not enough for the quiet constraint.** The three traversals above do not recover §1 and §2, and neither does a match that fires only when the failure already quotes the constraint. Two candidate paths are both required:

- **Referent match.** Propose a pair when a load-bearing turn's outcome record, residual payload, or live error text names a checkable referent (an exact identifier, path, quoted constraint, or error token) and a sealed turn contains that same referent. This is not an embedding neighbor and it is not `similar_to`. Turns with no criterion id are eligible.
- **Constraint scan.** On append, a turn that records an operator constraint, an environment condition, or a standing "never" commitment (WP §5.1) MUST be marked `constraint-fact`, including when it has no criterion id, and its turn id MUST be appended to the plan's `constraintLedger`. The ledger is the retention horizon: every marked turn in the active plan stays addressable. The hot posting list below is an accelerator. Falling off that list does not delete the ledger id. The failure text does not have to contain the constraint string. Paraphrase and a stub that dropped the fact do not remove the ledger id.
- **Rotation, not newest-only.** Each scan proposes `K_constraint` ledger entries that have not yet been judged against the current `failureEvidenceRevision` (provisional **`K_constraint = 4`**), oldest unjudged first, from a durable `constraintCursor`. `K_constraint` counts against the generation-wide `K_ref` budget below. Newest-four selection is non-conforming. Turn 4 is proposed when the cursor reaches it, including when newer marks exist and including when turn 4 is outside the hot list. A rejection suppresses a later proposal only when the stored revision equals the current `failureEvidenceRevision`. One full cursor cycle on that revision with no confirming edge records `constraint-scan-exhausted` for that revision. That record is not success, and it does not apply to a later revision.
- **Failure identity and claim set.** A live failure has `failureId` (the same turn id plus its evidence watermark) and a claim set with `claimSetVersion`. Each claim has a `claimId`. `claimSetStatus` is `empty`, `unclassified`, `partial`, or `complete`. An ordinary raw error starts as `empty` until a writer assigns claim ids, or as `unclassified` when text exists but no id has been assigned. Status and version are append-only. `failureEvidenceRevision` is the tuple (`evidenceWatermark`, `claimSetVersion`, `rawErrorHash`). A constraint judgment and a scan-coverage record bind to that tuple, not to the turn id alone and not to the watermark alone.
- **Revision invalidates rejections.** A new raw-error hash MUST bump `claimSetVersion` and MUST increment `evidenceWatermark`. Either change produces a new `failureEvidenceRevision` on the same turn. That new revision invalidates prior rejection records and prior coverage-stop records, including `constraint-scan-exhausted` and an HV `explained` or `adjudicated-empty` that names the previous tuple. A writer that bumps `claimSetVersion` and leaves the watermark number unchanged still invalidates those records, because the tuple differs. Confirming edges are not deleted by the bump. The `constraintCursor` for the new revision restarts at the oldest ledger entry. Resuming a cursor that was exhausted under the previous revision is non-conforming. Reopening the scan without invalidating rejections is non-conforming.
- **Partial explanation does not stop the scan.** One effective `explains` or `constrains` edge does not suppress the scan. The scan stops only when `claimSetStatus` is `complete` for the current `failureEvidenceRevision` and every claim id in that version is cited by an effective non-suspended edge, or when HV records `explained` naming that same revision. A stop record for an earlier revision does not stop the new one. An empty or unclassified set is not complete. "Every claim id is covered" is false when the set has no ids. HV may stop an empty set only with `adjudicated-empty` naming the current `failureEvidenceRevision`. Any other stop on an empty or unclassified set is non-conforming, and the cursor keeps walking.
- **Empty is not success.** If the ledger has no entries and referent match also proposes nothing, record `constraint-scan-empty`. That record is not a successful recovery. Neither is `constraint-scan-exhausted`.
- **Recall.** The denominator is the set of constraints an independent labeler marks as bearing on the failure, including constraints the generator never proposed. The numerator is how many of that set were proposed before the cursor completed a cycle that should have reached them. Candidate-source share is not this ratio. A recovery test fails if a labeled constraint is missing from the ledger, or if newest-only selection skips it while the cursor contract would have reached it.
- **Recovery acceptance cases.** Each of the following MUST propose the labeled constraint from the ledger before the cursor laps, and the recall denominator MUST include it even if a candidate list omitted it: an old constraint (turn 4, failure at turn 60, more than four newer `constraint-fact` turns); a paraphrased constraint with no shared string; a constraint whose fact is absent from `summaryText`; an unattributed constraint with no criterion id; a crowded-out constraint that is not among the `L` hot-list ids; a raw error with no claim id, whose claim set is `empty`, so the scan is not allowed to stop before the cursor reaches the labeled constraint. Revised-failure reconsideration: constraint C is rejected against the same failure turn at `evidenceWatermark` 7 and `claimSetVersion` 1. The raw error then changes and C now bears on that failure. `claimSetVersion` becomes 2 and `evidenceWatermark` becomes 8. No new failure turn is created. The rejection of C is invalid. The cursor restarts at the oldest ledger entry. C is proposed again before that new cursor laps. Leaving the watermark at 7 while bumping only `claimSetVersion` still proposes C again. Skipping C because it was judged at watermark 7 is non-conforming.

Both paths produce candidates only. Type and direction still require out-of-band judgment (§3, §3.1, §5.1).

**What the caps actually bound.** Eligibility and emission are different limits. The plan structure, including cross-phase criteria, decides which pairs may be considered. The caps decide how many are emitted and how the cheap walk is done. Pairing every turn on one criterion is quadratic. The generator MUST NOT emit all pairs. v0.1.7 said work per generation is O(K) and that revalidation cost is bounded by `J` rather than by graph size. v0.1.8 said session cheap work is O(turns times (D + K_crit + K_ref)) and that a shared evidence event enqueues in O(D). v0.1.9 said session cheap work is O(G_session times (T_new + K_gen + J + F_max) + E_events). v0.1.10 said session cheap work is O(G_session times (T_new times L + K_gen + J + F_max)). Those sentences are withdrawn. The v0.1.9 formula did not include posting-list reads, did not forbid a walk of E_live, and did not schedule first judgments. The v0.1.10 formula capped each list's length and did not cap how many lists a turn opens, and it left `E_events` out of the displayed expression.

The bounds that this algorithm actually has, counted in sealed turns, generations, evidence events, and dependents pulled from a cursor:

- **Generation-wide inspection.** A generation considers at most `T_new` newly arrived or newly imported turns (provisional **`T_new = 4`**). Further turns wait on the overflow cursor. Per-turn emission caps are not a license to process an unbounded catch-up inside one generation.
- **Generation-wide emission and attempts.** At most `K_gen` candidate pairs are emitted per generation (provisional **`K_gen = 16`**, of which at most `K_crit = 8` are criterion-graph hits and at most `K_ref = 8` are referent or constraint-scan hits). At most `K_attempt` pair inspections are attempted per generation (provisional **`K_attempt = 32`**), counting duplicates, deleted endpoints, ineligible edges, already-judged pairs, and emitted pairs. `K_gen` is not larger than `K_attempt`. Emission of zero pairs does not permit further attempts. Both caps are per generation, not per turn.
- **List openings.** A generation opens at most `P_open` posting lists (provisional **`P_open = 8`**): criterion-id lists, referent-token lists, and the load-bearing list count toward the same cap. A turn may name more keys than `P_open`. Unopened keys are not dropped. The durable `inspectionCursor` stores the turn id and the next unopened key ordinal and resumes there on a later generation. Opening every referent on a turn in one generation is non-conforming.
- **Posting lists and the ledger.** A criterion id, a referent token, and the load-bearing set each retain at most `L` turn ids (provisional **`L = 16`**). The load-bearing list is not an uncapped exemption. Ids past `L` wait on that list's cursor. The `constraint-fact` hot list has the same cap. The `constraintLedger` is not capped by `L`. Selection reads at most `L` ids from each list it opens, and it opens at most `P_open` lists. It does not scan every sealed turn and it does not scan `E_live`.
- **Degree.** A turn holds at most `D` effective edges (provisional **`D = 32`**). A new edge past `D` is not admitted. Its id is the next overflow cursor key.
- **Shared-evidence fan-out.** An evidence event bumps one watermark and returns. It does not enumerate dependents. An edge whose `evidenceVersion` is below that watermark is suspended (§5.2). A generation pulls at most `F_max` such edges from a durable cursor (provisional **`F_max = 32`**) for judgment. The rest stay suspended and ineligible. Their bodies are not copied into a second queue.
- **Model judgments.** At most `J` per generation and at most `J * T_j` estimated tokens (provisional **`T_j = 4096`**), with the slot split in §5.2.
- **Backlog and overflow.** Unjudged candidate bodies wait in a backlog capped at `M` (provisional **64**). When another body would exceed `M`, do not store it and do not drop it silently. Append one `overflow-cursor` audit record (event id, turn id, edge or ledger id) and resume from that cursor on a later generation. The cursor is FIFO by append order. A second unbounded queue is non-conforming. A drop without the cursor is non-conforming.
- **Inspection is the capped reads above.** A generation reads at most `T_new` new turns, opens at most `P_open` lists, reads at most `L` ids from each opened list, attempts at most `K_attempt` pairs, emits at most `K_gen` pairs, pulls at most `F_max` watermark hits, and performs at most `J` judgments. It does not walk `E_live`. Seal and tombstone checks for the sent assembly happen under the send fence (WP §6.1) for turns in that assembly only. `J_first` and the FIFO overflow rule below are unchanged.
- **Oldest-first service.** Whenever any never-judged candidate or overflow-cursor entry exists, the generation MUST spend `J_first` on the oldest such candidate before it spends remainder slots on newer ones. Under sustained arrivals that keep filling the tail, an older candidate at FIFO position `n` is judged within `n` generations. A scheduler that always serves new arrivals first is non-conforming.

`E_live` is at most `turns * D` under the degree cap. That cap limits stored edges. It is not a license to scan them each generation. `G_session` counts every tool-loop model call (WP §6.1). Session model-token cost is O(`G_session` * J * `T_j`). Session cheap work for this procedure is O(`G_session` * (`T_new` + `P_open` * `L` + `K_attempt` + J + `F_max`) + `E_events`). `E_events` counts watermark-bump events. Each bump is O(1) and is inside that term. Omitting `E_events` from the displayed bound is non-conforming. The term does not add an `E_live` walk. An implementation that scans `E_live` each generation is non-conforming, and this bound does not apply to it. The caps MUST be fixed before a linking run can pass or fail.

**Acceptance traces for the bound.** Multi-call turn: five tool-loop calls inside one unsealed turn are five generations, and the cheap-work total counts five, not one. High fan-out: one criterion-state change bumps one watermark; the handler does not list the dependent edges; at most `F_max` are judged in that generation; every other dependent edge is suspended and absent from the send. Sustained overload: arrivals that would emit more than `K_gen` candidates, with only `J` judgments, fill `M` and then advance `overflow-cursor`. No candidate influences a call from beyond the cursor, and none disappears without that audit record. First-judgment service: 30 never-judged candidates sit on the FIFO cursor while new candidates keep arriving. Each generation spends `J_first` on the oldest. Candidate 30 is judged by generation 30. A schedule that never reaches it while new work arrives is non-conforming. Many-referent inspection: one new turn names 1000 distinct referents. Almost every posting-list hit is a duplicate, a deleted endpoint, or an already-judged pair, and the generation emits zero candidates. That generation opens at most `P_open` lists and attempts at most `K_attempt` pairs. `inspectionCursor` stores the next unopened referent. Later generations resume there. Probing all 1000 lists in one generation is non-conforming even though emission is zero.

**A requirement-to-requirement dependency graph is not in those artifacts.** `docs/Project/TR-per-FR-Mapping.md` maps each FR to primary TRs and tests. `docs/Project/Requirements-Matrix.md` maps each requirement to a status and source files. Neither records an edge between requirements, a direction, or what following that edge would mean. An earlier draft named "requirement-to-requirement dependencies from the matrix" as a traversal this design could execute. It cannot, and the long-range candidates that exist only as cross-requirement dependencies are absent until an artifact declares them. That artifact would still be candidate generation under the rule above: a dependency between requirements proposes pairs, and the turn-level edge type and direction still need payload or outcome evidence. Semantics required before any such traversal runs: what the requirement edge asserts, which direction it has, and that it is a hint rather than an admission. Open question 12 records the gap. Inventing the graph inside the indexer would recreate the unsupported causal edge this section exists to avoid.

Turns bearing on no criterion are eligible for referent match and for the `constraint-fact` scan. A similarity fallback remains out of scope until it meets the §9 admission gate and answers the `similar_to` objection.

### 5.5 Lifecycle is append-only

An edge's history is a sequence of recorded transitions — proposed, confirmed, retracted, re-proposed — each with its reason and generation, not a mutated confidence column. Two reasons:

- **Auditability.** When a projection changes between generations, the operator can ask why, and the answer is a readable record rather than a diff of vanished state. Each transition also records the endpoint seal sequences it was judged against (§5.2), so a reseal is visible as an invalidation rather than as a silent reuse of the old judgment.
- **Free churn measurement, not free precision.** The lifecycle yields a retraction rate and a flip rate per generation with no extra harness. Those are churn metrics. They are not precision. A false edge may stay confirmed indefinitely, and a correct edge may be retracted because a reseal or a later turn changed its applicability. Proposal precision and admission precision are defined in §9. Both need labels from a party that did not propose the edge: operator review, or another adjudicator judging the sealed payloads. Using retraction rate as either ratio would make the §9 report invalid. See §9.

**Persistence is mandatory.** IMPL §3.2.1: set-based updates bypass the audit ledger. Edge transitions, scope and progress revisions, and outcome records (including explanation supersessions) MUST be written through the tracked save path, or through an equivalent append-only ledger that set-based updates cannot bypass. If that path is unavailable, the write MUST fail closed. A mutable confidence column, progress cell, or explanation body updated in place is non-conforming.

**Effective state.** Readers compute current state from the append-only sequence. The effective edge is the latest transition that has not been invalidated by a later seal mismatch or a later evidence change. A new transition MUST name the transition it supersedes. Earlier transitions stay readable. Admission, weight revision, and projection MUST use the effective transition, not a side column. The same rule applies to the progress series (§6.2) and to explanations (§7.2): a correction is a new record that names the record it supersedes.

---

## 6. Goal metrics: scope and progress

### 6.1 The measurement that weight has been missing

WP §5 scores weight as relevance to unfinished work. This section defines the only progress quantity that may revise that weight.

**The goal is the plan, not the turn.** This is the unit correction that makes the rest of §6 tractable, and an earlier draft got it wrong by treating each turn as a thing to be scored. A turn has no goal of its own; it either moves the plan or it does not. Byrd Development Process v4 already fixes the unit: "Completion is not a definitive end state, simply a declaration of a set of requirements and acceptance criteria and proof of achievement of both" (`docs/Development-Process-draft-v4.md`, Planning). A plan declares the criteria; a turn contributes proof.

**Authoritative definitions (binding for weight and packing).**

The stored object is a **per-criterion state vector** for the active plan. Each in-scope acceptance criterion has one state: `passing`, `failing`, `skipped`, `untested`, or `blocked-no-test` (declared, but no bound test). A criterion id appears once. A slice view is a filter of this vector. It is not a second progress number for weight.

- **Scope** is the count of criteria in the vector, including `failing`, `skipped`, `untested`, and `blocked-no-test`.
- **Progress** is the count of criteria whose effective state is `passing`. `skipped` is not passing. `failing`, `untested`, and `blocked-no-test` are not passing.
- A **tier-2 checkpoint** may update every criterion the suite actually executed. Criteria the suite does not name keep their previous state. The revision records which ids ran.
- A **tier-3 targeted run** updates only the criteria it executed. It MUST NOT mark an unrun criterion `passing` or `failing`, and it MUST NOT replace the vector with the partial count.
- The **effective state** of a criterion is the latest append-only revision for that id (§5.5). A mutated cell is non-conforming. "Progress is an estimate" in §6.2 means a revision of this same vector can be wrong. Introduced versus revealed classifies a downward revision. It is not another progress series.
- A turn's **reading** is the set of per-criterion attributions after a tier-2 or tier-3 revision, plus the scope and passing counts. Each criterion the turn affected stores `{criterionId, before, after, class}` where class is `gain`, `loss-introduced`, `loss-revealed`, or `none`. The net counts do not replace that set. §6.5 reduces the set into weight and warnings. A `gain` MUST NOT delete a `loss-introduced` row.

**Evidence state is not the test observation.** Each criterion stores two records. The **test observation** is `pass`, `fail`, `skipped`, or `not-run`. The **satisfaction** state is what progress counts, and only HV writes it: `passing`, `failing`, `skipped`, `untested`, `blocked-no-test`, or `disputed`. Progress counts `passing` only. A `pass` observation is not `passing`. `disputed` is not `passing`.

Every observation and every satisfaction revision binds five artifact versions plus the observation revision: `codeHash` of the exercised files and their declared dependency closure, `testHash` of the bound test, `criterionHash` of the criterion statement, `catalogHash` of the catalog row, `environmentId` of the named toolchain and fixtures, and `observationSeq` of the observation HV adjudicated. A revision that omits any of these is non-conforming and MUST NOT be treated as current. A newer observation on the same five artifact versions gets a higher `observationSeq`. The historical satisfaction row stays readable and is labeled with the `observationSeq` it adjudicated. It is not current satisfaction once a newer contrary observation exists.

**Precedence.** HV satisfaction outranks an observation only for the `observationSeq` it names. A later run that observes `pass` on the same binding MUST NOT overwrite an HV `failing` or `disputed` satisfaction. Overwrite of an adverse judgment requires a new binding together with a new HV adjudication, or an HV record `addressed` that names the prior adverse revision. A newer observation of `fail` or `skipped` on the same five artifact versions, with a higher `observationSeq` than the one current satisfaction names, suspends that satisfaction immediately: current satisfaction becomes `disputed` before the next HV record. Progress MUST NOT keep counting the older `passing` row.

**Stale and disputed.** If any of the five bindings changes and the criterion is not re-executed and re-adjudicated on the new tuple, the old observation becomes `stale` and satisfaction becomes `disputed`. A partial run updates observations only for criteria it executed. It still applies the binding check to criteria it did not execute. An unexecuted criterion whose bindings did not change keeps its satisfaction. An unexecuted criterion whose shared dependency changed becomes `disputed` and drops out of the passing count.

**Acceptance cases.** Passing-but-inadequate: the bound test observes `pass`, and HV writes satisfaction `failing` with reason `inadequate`. The criterion is not in the passing count. A later run of the same test hash that again observes `pass`, with no new HV adjudication and no `addressed` record, leaves satisfaction `failing`. Targeted run after a shared dependency change: criterion X is `passing` on binding B1. A tier-3 run executes only criterion Y and changes a file in X's declared dependency closure, so X's `codeHash` moves to B2. X was not executed. X's satisfaction becomes `disputed`. The passing count falls by one until X is re-executed and HV adjudicates B2. Same-binding pass-then-fail: observationSeq 1 is `pass` on binding B and HV writes `passing` for that seq. observationSeq 2 is `fail` on the same five artifact versions. Current satisfaction becomes `disputed` before any new HV record. The passing count drops. The observationSeq 1 row remains readable and is not current.

The following are not authoritative progress and MUST NOT revise `weight` or change packing on their own: WP §5.1 relevance (the quantity `weight` already is); a raw test observation with no HV satisfaction on the current binding; a tier-1 pending verdict; an actor intent claim; a completion ratio; the prose label "progress narrative."

**Proposal.** Track those two series per plan. Scope rises when the plan grows or proves more complex than believed. Progress rises when another in-scope criterion's bound test is passing on the latest authoritative reading.

BDPv4 plans in this repository carry Functional, Technical and Testing requirements with stable identifiers, and `docs/plans/mcp-memory-002-ac-catalog.json` enumerates 283 acceptance criteria that each bind a statement to a requirement, a `TEST-*` id, a named test method, and a slice. The counts above are commensurate by construction (the §6.3 commensurability requirement is satisfied by the artifact), and BDPv4's phase-exit gate ("the full test suite for the current increment plus all prior work must be green") is one way to produce a tier-2 reading. That reading is plan-level. It is not by itself a per-turn verdict, and `Validate-RequirementsTraceability.ps1` does not compute it (§6.4).

Two independently moving lines, rather than a single percent-complete, which is the property that makes the pair robust to a goal changing underneath the measurement. A completion ratio alone cannot distinguish "advanced" from "target moved." This is the burn-up / earned-value shape, and that lineage should be cited if this enters the whitepaper — WP §12 is frozen for v0.1.x, so it is named here as a concept and needs a reference in v0.2 rather than a citation added now.

Both values belong to the plan. A turn's reading is the pair of plan-level deltas attributed to it:

| Scope | Progress | Reading |
|---|---|---|
| flat | up | Genuinely useful turn |
| flat | flat | No net movement. Harm still applies if any criterion is `loss-introduced` (§6.5) |
| up | flat | Discovery or decomposition — the goal was larger than believed. Not harmful |
| up | up | Work plus discovery |
| flat | **down** | Regression — but see §6.2 before calling it harm |
| **down** | up (ratio) | Descoping. Must be justified, never rewarded — see §6.3 |

### 6.2 Progress regression does not imply a harmful turn

A regression is a downward revision of authoritative progress as §6.1 defines it. The rule "progress regresses with scope constant, therefore the turn was harmful" is not sound as stated, and the correction matters because the exception is high-value.

A turn that discovers earlier work was wrong reduces progress without changing scope. It is also frequently the most valuable turn in a session. Under the unqualified rule it is scored as harm, which optimizes directly against error detection.

BDPv4 states this outcome as an expectation rather than a hazard, under "Resolving Defective Requirements": writing tests against acceptance criteria surfaces "paradoxes created by mismatched priorities, ambiguity and incorrect rules," and "Expect to refine requirements in each iteration... Expect to touch previously written code... This is not a failure of the process, but validation that the core philosophy of iterative improvement is alive and working." The argument below is therefore a restatement of existing process doctrine in metric terms, not a new finding, and it is cited here as precedent rather than presented as a discovery.

The distinction is **introduced** versus **revealed**:

- *Introduced* — the turn made things worse. Progress genuinely fell. This is harm.
- *Revealed* — the turn discovered that a previous progress reading was overstated. Nothing got worse; the measurement was corrected.

This forces a useful admission: **progress is an estimate carrying error, not ground truth.** So the progress series must be append-only revisions with attribution, never a mutated current value. Overwriting destroys the only evidence that distinguishes the two cases after the fact.

Worked example from this document's own history: the whitepaper and implementation proposal absorbed five corrections in one session, each one reducing apparent progress — sections reopened, recommendations reversed, claims retracted — while raising actual correctness. A metric that scored those as harm would have taught the agent not to look.

### 6.3 Who measures progress: Hostile Validation, not the actor

The acting agent must never score its own progress. It is an unreliable narrator with a standing incentive to report advancement, and that is structurally the same defect WP Principle 8 exists to block — a self-produced number presented as an outcome.

**Progress and regression readings are outputs of Hostile Validation.** WP §11 defines HV as an accuracy and completeness gate. Today it is applied as a document-level review ritual; here it becomes a runtime component that adjudicates turns. The actor does the work; an adversarial validator, which did not do the work and is not rewarded for it having gone well, determines what was accomplished.

This is the same structural move as §5.1, and the two should be stated as one rule:

> **No component may validate using the artifact it produced.** Link revalidation is blind to the projection it influenced (§5.1); progress adjudication is blind to the actor's own account of its work (§6.3).

**Claim versus verdict.** The actor is still the best source of *intent* — what it was trying to do and why it believed the approach would work — and that knowledge is not recoverable from a transcript afterwards. So the two roles split:

- The actor records an **intent claim**: the goal targeted, the criterion it expected to satisfy.
- HV records the **verdict**: what actually resulted, and the progress or regression delta.

One direction of divergence is the harm case in §6.2: the actor claims a criterion was met and HV finds it was not, detected without anyone having to trust the actor. Agreement is not a failure mode. When the claim is accurate and the work is straightforward, a competent independent validator should agree. Near-zero divergence is compatible with a healthy batch and with rubber-stamping; the rate alone cannot tell them apart, and treating agreement as capture would reward false adverse verdicts (§9).

**HV is not ground truth either.** It is an adversary with its own error modes, false accusation among them, and it may be wrong in the other direction. So verdicts are evidence rather than fact: append-only (§6.2), and anchored to checkable referents rather than to HV's unsupported opinion. WP §4.2's `pin` classes already include **acceptance**, which supplies the anchor — HV's job is to check acceptance criteria, tests, builds and artifacts, and to label any interpolation between them as interpolation rather than as measurement.

**Where the gaming surface moves.** HV closes self-reporting; it does not by itself stop an actor from arranging favorable criteria. §6.4 closes the part planning can close: criteria precede the turn, and dropping an `FR-` (and, in strict mode, a `TR-` or `TEST-`) from the requirements documents fails the traceability script. It leaves the catalog-to-test surface open. Deleting an acceptance criterion, or weakening the test bound to it while the matrix row remains, is not a script failure.

**Cost and cadence.** Plan-level progress in §6.1 is criteria with passing tests. BDPv4 requires the full suite for the current increment plus all prior work to be green before an implementation phase exits (`docs/Development-Process-draft-v4.md`, Implementation). It does not require that suite after an ordinary turn inside the phase. A phase-exit run is one reading for the phase. It cannot attribute a pass or a failure to an individual turn: every turn since the previous green reading is a candidate cause. Reusing that eventual run as a per-turn verdict would invent attribution the runner does not have. Running the full suite after every turn would be a new cost, and it would contradict any claim that per-turn readings are free.

Per-turn HV verdicts use three tiers. The cost sits with the tier that actually ran:

1. **Intent only, between runs.** After an ordinary turn the actor's intent claim is recorded and the HV verdict is `pending`. No suite is launched. This tier does not fill §6's scope/progress table and it is not a progress delta. It also does not write satisfaction. A test observation, if one was already stored, stays an observation (§6.1).
2. **Batched attribution, at a checkpoint.** The next scheduled run - a slice checkpoint the plan already names, or the phase-exit suite - produces one scope/progress reading for the batch. HV attributes the delta to turns inside the batch. The candidate set for a criterion is the union of three sets, since the previous authoritative reading. (1) Turns that modified that criterion's bound test or the code the test exercises. (2) Discovery turns: turns that did not modify that code, whose sealed residual facts or outcome record cite the criterion, a requirement it decomposes, or the defect being revealed, and that HV labels `discovery` or `revealed` for that criterion. Scope-up with flat progress in the §6.1 table is this class. (3) Investigation turns whose intent claim names that criterion, when HV accepts the claim as discovery. The actor's claim alone does not attribute. Turns in none of these sets stay unattributed for that criterion. Discovery and revealed-regression turns are inside the set; they are not dropped because they never touched the test. Attribution is a judgment over that candidate set (§6.2, introduced versus revealed), not a property of the test runner. The suite is charged once per batch on the §9 HV adjudication cost, not once per turn, and not described as free.
3. **Targeted tests, when a turn-level verdict cannot wait.** A harm flag, link evidence that cites a criterion, or the §10 deadlock threshold may request the tests named by the affected criteria, not the full suite. That request is a cost BDPv4's phase-exit gate does not already pay. It is reported per request in the §9 HV adjudication cost. It is not the default tier.

Model adjudication remains what tests cannot decide: whether a passing test demonstrates its criterion, and the introduced-versus-revealed split inside a batch. That ordering follows WP §10 risk 6 ("v1 rules by default; judge on schedule or on budget pressure"). An earlier draft of this section called the phase-exit suite the per-turn base tier and said most readings are free. That collapsed a phase aggregate into a per-turn verdict. The phase-exit suite remains the BDPv4 gate and the plan-level reading in §6.1. It does not yield per-turn verdicts. If targeted runs or full-suite runs grow with turn count, adjudication has become the default tier. Nothing here claims HV is cheap or currently available; the existing document-level Perplexity HV remains blocked on an API key and must not be described as done.

**Commensurability.** Scope and progress must share units, or independent movement means nothing. If scope counts known acceptance criteria, progress counts criteria met. If scope is in estimated units of work, progress must be in the same units. Mixing "complexity" with "criteria completed" produces two series whose deltas cannot be compared.

**Descoping.** Removing scope inflates any completion ratio without work being done. Every scope change records direction, reason, and justification, and the ratio must never be a reward signal on its own. A scope reduction is a claim that work is unnecessary, and that claim is exactly as checkable as a progress claim. BDPv4 places that decision outside the actor entirely: "stakeholders need the flexibility to iteratively approve or deny continued resource expenditure towards completion." Descoping is a stakeholder act, and an actor-initiated one is a request, not a scope change.

### 6.4 Planning closes the criteria surface

Criteria authorship is not left to the turn that will be judged by it. BDPv4 requires the criteria to exist before implementation: "Planning results in a set of artifacts that capture Functional Requirements... Technical Requirements... Testing Requirements... and Iterative Phases," and "System components need to be discovered, designed and all public interfaces documented before writing implementation code." Plans in `docs/plans/` name their requirement families in the header and withhold `Done` until independent gates pass — `PLAN-LLMSTRATEGY-001-bdpv4.md` records "leave `Done: false` until Codex READY, tests Failed 0/Skipped 0, and hostile AGREE." Three gates, none of which the acting agent controls.

Two mechanical properties do the work that §6.3's adjudication cannot:

- **Criteria precede the turn.** An actor cannot mint a favorable criterion mid-turn, because the criterion set is a plan artifact authored before implementation and versioned with it. Scope changes are visible as diffs to that artifact.
- **Identifier coverage is machine-checked. Catalog-to-test coverage is not, and that surface stays open.** `scripts/Validate-RequirementsTraceability.ps1` compares identifiers in `Functional-Requirements.md`, `Technical-Requirements.md`, and `Testing-Requirements.md` with `docs/Project/TR-per-FR-Mapping.md` and `docs/Project/Requirements-Matrix.md`. It exits non-zero when an `FR-` heading is missing from the mapping or the matrix, and in strict mode when a `TR-` or `TEST-` identifier is missing from the matrix. It does not read `docs/plans/*-ac-catalog.json`, it does not resolve named test methods, and it does not compare an acceptance-criterion statement with the test that is supposed to demonstrate it. Quietly dropping an `FR-` from those requirement documents fails the script. Deleting an acceptance criterion from a catalog, or deleting or weakening its bound test while the corresponding `TEST-*` row remains in the matrix, still passes. That is the criteria-gaming surface this section is about, and the script leaves it open. Closing it would take a separate catalog-to-test check this addendum does not claim exists. Until that check exists, a catalog edit or a bound-test edit is visible to a reader of the diff and invisible to this gate.

**The real gaming mode is narrower than criteria invention, and it is already documented.** BDPv4's monitoring list names it: an agent stuck in "unresolved loops of failing tests... can lead to the agent marking a test as invalid so it can keep moving forward while trying to be a useful assistant, losing its identity as a precise software engineer in the process." That is criteria gaming as actually observed — not authoring an easy criterion, but invalidating a criterion that is bound to a test and blocking. A reader of the diff can see a test method named in the acceptance catalog changing state to skipped, deleted, or rewritten. `Validate-RequirementsTraceability.ps1` will not. This is why BDPv4's phase-exit gates count skips (`Failed 0/Skipped 0`) rather than only failures, and why a test-state change touching a catalogued method should be treated as a scope change requiring stakeholder approval rather than as ordinary refactoring. The gate that would fail the build on that diff does not exist yet; the stakeholder rule is the control that does.

**The residual is human.** BDPv4 does not claim automation closes this — its mitigations for all three monitored behaviors are an experienced human steering in real time, and for a session whose trust is broken, abandonment: "its impossible to fix the trust and get the model to behave correctly. Simply end that session, close that agent, and start over." Any claim that this addendum removes the operator from the loop would contradict the process it depends on.

### 6.5 How authoritative progress revises weight

Two layers. `scorerWeight` comes from WP §5.2 and does not include this section. `effectiveWeight` is `scorerWeight` after the rule for the **effective** verdict only: the latest superseding outcome record (§5.5, §7.2). Progress machinery MUST NOT write `weight` from a tier-1 pending verdict or from an actor claim alone. Floors and ceilings below are provisional, operator-adjustable, and MUST be fixed before a scoring run can pass or fail. They are absolute values on the independent scalar in WP §4.2.

A corrected verdict replaces the progress layer on the next generation snapshot (WP §6.1). It does not stack a new floor or ceiling on top of the old one. The previous ceiling or floor does not remain after the class changes.

- **Progress up** (scope flat or up, passing-count up), attributed to the turn: `effectiveWeight = max(scorerWeight, 0.60)`.
- **Discovery** (scope up, passing-count flat), attributed to the turn: do not lower `scorerWeight` because scope grew. If the discovery is the current blocker or the constraint the live failure cites, `effectiveWeight = max(scorerWeight, 0.60)`. Otherwise `effectiveWeight = scorerWeight`. The turn stays eligible for the constraint scan and the criterion graph (§5.4).
- **Introduced harm** (any per-criterion class `loss-introduced`, even when scope is flat and the passing count is flat or up): `effectiveWeight = min(scorerWeight, 0.20)`, and WP §6.2 applies (marked stub if unpinned; expanded pin plus harm marker if pinned). A `gain` on another criterion does not cancel this warning and does not replace the ceiling with the progress floor. The gain row stays in the audit. Floors and ceilings do not stack: when both a gain and an introduced loss are present, the ceiling is the progress layer and the warning stays.
- **Revealed regression**, on the revealing turn: do not apply the introduced-harm ceiling to that turn. If it is the current blocker, `effectiveWeight = max(scorerWeight, 0.60)`. Turns that held the overstated progress take the introduced-harm rule only when the effective verdict attributes `introduced` to those turns.
- **No attributed delta,** or a superseding verdict whose effective class is none of the above: `effectiveWeight = scorerWeight`. Remove the harm annotation only when this bullet is a superseding verdict, not when a floor merely expires. A pin stays expanded. An unpinned turn is no longer forced to the harm stub when the effective class is not `introduced` or when §7.4 has retired the warning.

**Floors and ceilings expire without a false verdict.** A floor or ceiling applies only while the contribution is still active. Active means the credited criterion's satisfaction is not `passing` on the current binding, or the turn is the current blocker, or the live failure cites the turn. When the credited criterion is `passing` on the current binding and the turn is neither the blocker nor cited by the live failure, the next snapshot sets `effectiveWeight = scorerWeight`. The progress-up or discovery record stays in the audit log. Expiration is recomputation. It MUST NOT be written as a superseding verdict that calls the old credit false. The same recomputation drops an introduced-harm ceiling when §7.4 retires the warning. The `introduced` verdict stays in the audit log.

Floors and ceilings still do not stack. A superseding verdict replaces the progress layer. An expiration removes the layer. Neither adds a second floor onto a leftover one.

**Acceptance traces.** Verdict correction: HV supersedes `introduced` with `revealed`. The next snapshot drops the 0.20 ceiling and does not keep a 0.60 floor from an earlier credit unless the revealed bullet above applies. Completion-driven relevance loss: a turn's credit is real, `scorerWeight` moves from 0.50 to 0.00 after the credited criterion becomes `passing`, and the turn is not the live blocker. The floor expires. `effectiveWeight` is 0.00. Discretionary demotion compares that value with `weightAtLastStateTransition` (WP §6.1) and MAY demote. The audit row for the original progress-up credit remains. Mixed outcome, one plan, one turn: criterion A moves from `failing` to `passing` (`gain`) and criterion B moves from `passing` to `failing` with class `loss-introduced`. Scope is flat and the passing count is flat. The turn still takes the introduced-harm ceiling and an active harm marker. The A gain remains in the audit and does not clear the marker.

If HV supersedes `introduced` with `revealed` or with no delta, the next snapshot uses the new `effectiveWeight` and the new annotation. The old 0.20 ceiling is not left in place.

A weight write from this section is allowed during re-admit cooldown. `projectionState` still follows WP §6.1: the effective harm class is a mandatory override; discretionary state changes wait on hysteresis and cooldown.

---

## 7. Turn outcome records: result plus explanation

**Proposal.** Each turn carries a record of its result together with an explanation of why that result occurred. The explanation, not a bare number, is the signal that determines whether the turn advanced or regressed the goal.

This is what makes §6 operable. A progress delta on its own cannot distinguish introduced from revealed regression (§6.2), nor justify a scope change (§6.3). The explanation supplies the attribution that both need.

Per §6.3, the explanation is an **HV output, not an actor output**. The actor contributes its intent claim; the adversarial validator accounts for what actually happened. An earlier draft of this section had the actor explaining its own result, which reintroduced the exact gaming surface §6.3 exists to close — and free-text self-justification is *worse* than a self-reported number, because prose can persuade in ways a number cannot.

### 7.1 Explanations must be structured claims, not prose

Moving authorship to HV removes the incentive to flatter; it does not by itself make an explanation checkable. An adversary can also be confidently wrong, and a fluent accusation is as unfalsifiable as a fluent excuse.

So explanations — from either side — are constrained to verifiable referents: which acceptance criterion changed state, which test moved, which prior turn is contradicted, which file or identifier is implicated, which goal is affected and in which direction. Then the account is checkable rather than merely persuasive, and one citing nothing checkable is visibly weak on its face. This applies symmetrically to the actor's intent claim and to HV's verdict; neither gets to assert an outcome it cannot point at.

#### 7.1.1 The explanation's destination is the plan, not just the metric

BDPv4 already uses explanations this way, and for a purpose stronger than scoring: "ask the model what caused it to go on a tangent, and how the requirements and workspace guidelines could have guided it towards the correct path to take. Then have **THAT** model update the documentation and guidelines." The explanation is an input to criteria refinement.

That closes a loop §6.4 would otherwise leave open. Criteria precede the turn, so an actor cannot revise them to suit its own result — but criteria are also frequently the thing at fault, which is BDPv4's "Resolving Defective Requirements" case. Routing explanations into requirement and guideline revision lets defective criteria be fixed without letting the actor fix them *for its own verdict*: the revision is a scope change to a plan artifact, visible as a diff and subject to the stakeholder approval in §6.3.

### 7.2 Explanations are append-only evidence

An explanation is written at the time, from what was then known, but the signal it feeds is retrospective. Later turns may show it was mistaken. It must therefore be preserved and reinterpreted, never rewritten in place: the record is evidence-at-the-time. A correction is a new record that names the record it supersedes (§5.5). The effective explanation is that latest record. This is the same append-only requirement that §5.5 places on edges and §6.2 places on the progress series, and for the same reason - retrospection needs an unedited past.

### 7.3 Consequence: `summaryText` keeps residual payload facts

WP §4.2 carries `summaryText` as the mid-tier stub, produced by summarizing the payload. The outcome record is an input to that stub. It is not a sufficient source for it.

An earlier draft said the pairing of intent claim and HV verdict should replace the payload summary: it is already compressed, it avoids a second summarization pass, it is stable across regenerations, and each half has an author the payload summary lacks. That replacement drops facts that are neither the attempted criterion nor the outcome. WP §5.1 treats those facts as high-weight inputs: an operator constraint that has not been externalized to memory, the exact error text of an unresolved blocker, an environment condition, the latest tool result the next action depends on. WP §5.2's v2 judge re-scores omitted turns from candidate stubs, not from full payloads (§1). A stub that kept only intent and verdict makes those facts invisible to scoring and to the live projection, which removes the quiet constraints this addendum is trying to keep recoverable.

**Requirement.** `summaryText` is derived from the outcome record and from residual payload facts. The outcome record contributes what was attempted, what HV judged, and the checkable referent (§7.1). The payload contributes facts the outcome record does not state. A derivation that cannot point at those facts in the sealed payload is incomplete. Intent plus verdict is the outcome half of the stub, not the stub.

The cost and stability arguments still apply to the outcome half. They do not justify omitting the payload half. Inside the outcome half, both roles still matter: an HV verdict alone loses why the approach was chosen, which is what stops the same reasoning being retried; an actor claim alone is the self-assessment §6.3 rejects.

This is a design change to the summarized tier and should be evaluated as one, not adopted by assumption.

### 7.4 Consequence: introduced-harm is a marker, and pins stay expanded

WP §4.2 allows `expanded`, `summarized`, `omitted`. None of those names is "actively misleading." Introduced-harm (the `introduced` class in §6.2) is an annotation on one of those states, not a fourth `projectionState`. Open question 2 is closed by this rule. The rule is WP §6.2; this section restates it.

- An **unpinned** introduced-harm turn MUST be `summarized`, with a marked stub: the approach, that HV judged it introduced a regression, the checkable referent (§7.1), and a statement that the payload is not the recommended next action. It MUST NOT stay `expanded`. It MUST NOT be silently `omitted`, because omission leaves the failed approach available to retry with no marker.
- A **pinned** introduced-harm turn (operator, system, or acceptance pin) stays `expanded` until unpinned. The same harm marker is attached beside the expanded payload and charged against `B`. The pin is not stubbed, omitted, or suspended. If the pin plus the marker exceeds `B_turns`, WP §6 fails closed.
- A **revealed** regression is not introduced-harm and does not force either bullet.

The marker is what tells the model not to treat a pinned payload as the next action, while Principle 5 keeps the pinned text verbatim. Stubbing a pin would hide content the pin exists to keep. This is the same precedence ADD §3.1 already uses for `invalidates`.

**Active warnings only.** A harm marker or an invalidation marker is part of the mandatory set only while its `warningState` is `active`. Active means the warning still prevents a current retry: the plan is not retired, and HV has not recorded `repaired` for that verdict on a criterion whose satisfaction is `passing` on the current binding. Audit history is the verdict row plus every later record. Retirement does not delete it and does not relabel a true `introduced` verdict as false.

**Retirement.** Append `warning-retired`. Any one of these is sufficient:

- HV records `repaired` and the affected criterion is `passing` on the current binding.
- The plan or slice that made the warning load-bearing is retired.
- **Consolidation.** Provisional **`W_max = 8`** active markers. When active markers exceed `W_max`, the oldest active markers whose criterion is already `passing` may collapse into one consolidated marker only after the replacement check below passes. Their verdict rows stay.
- **Standing memory.** A standing memory already in the prefix may replace the per-turn marker only after the replacement check below passes.

**Replacement meaning.** A consolidated marker or a standing memory is an adequate replacement only when the sent text itself contains all four of these, not merely an id: the prohibited approach or obsolete result; whether the meaning is harm or invalidation; a checkable referent; and an explicit statement that this is not the next action. Retirement by consolidation or memory transfer is refused when any of the four is missing. An id-only replacement is non-conforming, and the individual marker stays active and spill-protected. `repaired` and plan retirement do not need a replacement, because the retry obligation has ended.

**Presence in each send.** A warning retired only because of a replacement stays retired for a generation only when that generation's sent assembly still contains an adequate replacement for it. If the standing memory leaves the prefix, or the consolidated marker no longer carries the four fields, the individual warning is active again before send. Reactivation is mandatory and spill-protected. Pins stay expanded.

Retired markers MUST NOT be spill-protected and MUST NOT by themselves fail the budget closed. Active markers, including the consolidated marker, any dependency-gap marker (§3.1), and any reactivated marker, stay spill-protected (WP §6.3). Retiring a marker does not unpin. Pins stay expanded.

**Acceptance cases.** A long history contains more resolved regressions than fit in `B` as individual raw markers. Each of those regressions is `repaired` and its criterion is `passing`. After retirement, the generation sends a projection. It does not fail closed because of the retired set. One warning that is still active remains in the mandatory set, survives spill, and is visible on the sent prompt. Pins in that same assembly stay expanded. Id-only replacement: a consolidated marker or memory that cites only a warning id does not retire the individual marker. Later removal: a warning was retired because an adequate standing memory was in the prefix; the next snapshot whose prefix lacks that memory reactivates the per-turn marker before send.

### 7.5 Consequence: explanations are an indexing substrate, not a sufficient one

Explanations name causes, so they are a useful index for the admissible edge types, which are causal or dependency assertions. `shares_root_cause` in particular is close to a comparison of two explanations. They are not a sufficient index. Residual payload facts that never entered the outcome record (§7.3) are the constraints a link may need to cite, and discovery over explanations alone will miss them. Candidate evidence is the outcome record plus those residual facts, still judged out of band against the seals (§5.1), not against the projection. Cheaper than indexing every byte of every payload is not the same as indexing only the verdict.

---

## 8. Storage notes against the baseline

**Existing precedent for edge shape.** `GraphRelationshipEntity` (`src/McpServer.Storage/Entities/GraphRelationshipEntity.cs`) is a directed, typed, workspace-scoped edge with a `RelationshipType` string, a `Weight` double, an optional `Description`, and a `Metadata` JSON blob. That is close to the shape §3 needs.

**But it is not reusable as-is.** Its endpoints are `GraphEntityEntity` nodes via `SourceEntityId` / `TargetEntityId` foreign keys, not session turns. Turn-to-turn edges need their own table modeled on this precedent rather than rows in this one.

**No embeddings over turns.** The only embedding column found at the baseline is a nullable `Embedding` BLOB added to `Chunks` (migration `20260216171000_AddEmbeddingColumn`). Candidate generation cannot assume a vector index over turns exists. §5.4 takes the non-vector route deliberately rather than as a fallback: traversal over requirement and acceptance-criteria identifiers needs no embeddings, so a first cut requires no new index and no migration for one.

**Tombstone registration is mandatory, per IMPL §3.2.3.** Every table introduced here — edges, edge transitions, goals, scope/progress series, outcome records — is subject to the trap that section documents: `SoftDeleteTurnRowsAsync` and the revival path name their targets explicitly and discover nothing, so new tables keep `IsDeleted = false` when their turn is deleted.

This is more dangerous for links than for the sealed-turn table. An edge is *traversed automatically*. An unregistered edge table becomes a side channel that silently re-admits content from a deleted turn into a live projection — the operator deletes a session and the material keeps arriving through the index. IMPL §3.2.3's two remedies apply to every new table: register it on both the tombstone path and the revival path, and join reads so visibility follows the turn rather than a flag a future table might forget to set.

For an edge the join is stricter than the single-turn join IMPL §3.2.3 states for seal and projection-state rows. Traversal, link admission, and projection reads treat an edge as ineligible unless **both** the source turn and the target turn are non-deleted. Filtering one endpoint leaves the side channel open. A live source pointing at a tombstoned target can still inspect or re-admit that target; a live target reached from a tombstoned source is the same hole in the other direction. §3.1 admits a specific endpoint, so either end can be the turn the projector reads. An edge with either endpoint tombstoned is not a candidate and not an admission, regardless of type, confidence, or which end is live.

**Deletion is a snapshot input, and the send fence is the commit point.** WP §6.1 freezes the tombstone set with the other inputs. The final read and the transport handoff share one fence. A tombstone, reseal, evidence change, or superseding verdict cannot commit in the gap after the final read and before handoff. If that mutation wins the fence, the handoff is cancelled. If the handoff wins, the mutation has not committed and applies at the next generation. A published call that contains an endpoint whose tombstone committed before handoff is non-conforming. Using a pre-tombstone assembly after that commit is the side channel this section forbids. A generation interrupted before handoff is not published. The required traces are tombstone-before-send, reseal-before-send, concurrent supersession, interrupted generation, and the after-read-before-handoff race, as WP §6.1 states them.

**Audit path.** Per §5.5, edge transitions, scope and progress revisions, and outcome records MUST be written through the tracked save path (IMPL §3.2.1) or an equivalent append-only ledger. Set-based updates that bypass the ledger are non-conforming, and the write fails closed when the conforming path is unavailable. Effective state is the latest superseding record, as §5.5 defines it.

---

## 9. Evaluation hooks

This capability must not be adopted on the argument that it sounds right. Hooks into WP §9:

- **Re-admit usefulness (WP §9.1 metric 5)** is the natural home: when a turn is admitted by link, did the subsequent action correctly use it? Report link-admitted and score-admitted slices separately. The link-admitted slice has its own pass bar below.
- **Edge retraction rate** — retractions per generation, from §5.5's append-only lifecycle. This is a churn metric. It is not precision. A rising rate means edges are being withdrawn. It does not say whether the withdrawn edges were false, or whether the edges that remained confirmed were true. A false edge can sit confirmed forever; a correct edge can be retracted because its applicability changed.
- **Proposal precision** is the fraction of proposed edges that an independent labeler marks true. The population is every proposed edge in the sample, including edges that never admitted a turn. The labeler is not the component that proposed the edge. The label is against the sealed payloads current at judgment time, not against later retraction. This number is reported. It is not the adoption gate.
- **Admission precision** is the fraction of edges that admitted a turn into at least one assembly and that the same kind of labeler marks true. The population is admitted edges only. A high proposal precision does not pass this metric. It is not `1 - retraction rate`.
- **Adoption gate (predeclared).** Live link admission stays off until both bars below pass. There is no live trial in the production session: shadow assemblies are not copied into the model-facing projection, do not charge the link sub-cap, and are not shown to the actor. The frozen record for a gate run is the workload id, the generator version, the judge version, and the config hash (`K_*`, `J`, thresholds, `W_max`, `Dep`). A pass does not transfer to another tuple.
- **Predicted usefulness is not enablement.** The shadow log records whether an edge would have admitted a turn into a shadow assembly. **Predicted usefulness** is a labeler's judgment that the already recorded action was compatible with that turn. The action was generated without the admission, so the label is a prediction. It MUST be reported under that name. It MUST NOT be called WP §9.1 metric 5, and it MUST NOT by itself turn live admission on. Admission precision on the shadow-admitted population remains a required bar: at least 50 proposed edges, at least 30 shadow-admitted edges, admission precision ≥ 0.90. Thirty easy admissions do not pass the gate if recovery recall fails.
- **Observed-outcome gate, required before general enablement.** From the same frozen prefix, run two isolated replay continuations: one with link admission, one without. Production admission stays off; the replay is not the live session. An adjudicator that did not produce either continuation scores whether the with-admission continuation actually used the admitted turn. That score is observed usefulness. Provisional bar: ≥ 0.80. The without-admission continuation is the context-conditioned scalar baseline (§2). **Recovery coverage** uses the §5.4 label set (old, paraphrased, summary-lost, unattributed, crowded-out, and a raw error with no claim id). Recall on that denominator, which includes constraints the generator never proposed, MUST be ≥ 0.80. Below either observed bar, live admission stays off.
- **Requalification.** A material change to the generator, the judge, any `K_*`, `J`, a threshold, `W_max`, `Dep`, or the config hash voids the pass and turns live admission off until this gate is repeated on the new tuple. Shadow logging continues either way.
- **Edge flip rate** uses the published snapshot sequence only, so two evaluators of one trace get one result. The admission bit is 1 when the effective edge is admitting and 0 otherwise (`degraded` and suspended are 0). Denominator: edges whose bit existed on published snapshot G-1. If that count is 0, do not compute a rate; record `empty-edge-denominator`; the stability gate passes for this metric. Numerator: edges whose bit at G differs from the bit at G-1 for edge-layer cause `edge-score` (WP §6.1, §5.3). Intra-generation records do not add extra flips; only the bit on the published snapshot counts, once. `edge-invalidation`, `edge-suspend`, `edge-reconfirm`, `edge-degrade`, and `edge-budget` are mandatory. They are `mandatory-edge-flips` and are outside the numerator. `edge-score` maps to turn cause `link-admit` or `link-withdraw` and to `originCause` `edge-score`. Those turn causes stay outside the state-flip numerator. They stay inside the token-replacement numerator when `originCause` is `edge-score` (WP §9.1 metric 3). Dropping that token churn because the turn-layer label is mandatory is non-conforming. Mandatory origins (`edge-invalidation`, `edge-suspend`, pin changes, phase boundaries) stay outside the token numerator and are reported in `mandatory-token-count`. A generation whose trace causes are only mandatory is `mandatory-only` and is outside the mean. A generation with `phaseBoundary: true` and a `phaseId` (WP §9.1) is outside the mean. No other exclusion is allowed. Provisional bar: ≤ 0.10 when the denominator is at least 1. The rate is instability, not precision. Active-context token replacement (WP §9.1 metric 3) measures whether the live tail was replaced. If every generation in the session is excluded, do not compute a session mean. Record `empty-aggregate`. The stability gate passes. A missing mean is not a fail and is not a silent skip. The projector sets `phaseBoundary` true if and only if the plan phase id differs from the previous published snapshot (WP §9.1).
- **Mixed-cause expected results.** These numbers are the pass/fail oracle for WP §9.1 metrics 2 and 3 and for the edge-flip rate. State-flip: 1 discretionary `score` flip and 1 mandatory `link-admit` out of 10 previous active turns is 1/10 = 0.10, passes ≤ 0.10, and records `mandatory-transition-count` 1. The state-flip denominator stays the previous active set. Token replacement, mandatory-only: 40 of 100 previous-tail tokens change, all with `originCause` `edge-invalidation`. The generation is outside the mean. If it is the only generation, record `empty-aggregate` and the gate passes. Token replacement, mixed: 30 tokens with `originCause` `score` and 40 tokens with `originCause` `edge-invalidation` out of 100. The rate is 0.30 and fails ≤ 0.25. The 40 mandatory tokens are outside the numerator. Edge flip: 1 `edge-score` flip and 1 `edge-suspend` out of 10 prior edges is 1/10 = 0.10, passes ≤ 0.10, and records `mandatory-edge-flips` 1. A scorer that puts `link-admit` in the state-flip numerator, or that fails an `empty-aggregate` session, is non-conforming. Large-source discretionary withdrawal: 100 edge bits exist on the previous snapshot. One admitted source is 90 of the previous tail's 100 tokens. After cooldown, `originCause` `edge-score` withdraws that edge and `turnCause` is `link-withdraw`. The source has no independent retention and leaves the tail. Edge-flip rate is 1/100 = 0.01 and passes ≤ 0.10. Token-replacement rate is 90/100 = 0.90 and fails ≤ 0.25. The generation is not `mandatory-only`. A scorer that drops those 90 tokens from the token numerator, or that passes the token gate by excluding the generation, is non-conforming. The same withdrawal with `originCause` `edge-invalidation` reports the 90 tokens in `mandatory-token-count` and does not by itself fail the token bar. Pin changes and `phaseBoundary` generations stay outside the token numerator. Unchanged-state rewrite: turn T is `summarized` at G and is the whole previous tail, 100 tokens. At G+1, T is still `summarized` and still in the tail. No pin, edge bit, safety withdrawal, or phase boundary changes. The summary is regenerated and 90 previous tokens are absent. The trace has a `rendered-change` event with `previousRenderId`, `currentRenderId`, and `originCause` `summary-rewrite` on those 90 tokens. Metric 2 numerator is 0, so the state-flip rate is 0 and passes. Metric 3 is 90/100 = 0.90 and fails ≤ 0.25. The generation is not `mandatory-only`. Reusing T's earlier summarize cause, omitting the event, or passing metric 3 by exclusion is non-conforming. Mixed spans, same retained turn: 30 removed tokens are `summary-rewrite` and 20 removed tokens are `warning-retirement`, out of a 100-token tail. Metric 2 numerator stays 0. Metric 3 numerator is 30, rate 0.30, and fails ≤ 0.25. The 20 marker tokens are `mandatory-token-count` only. One primary `turnCause` for both spans is non-conforming.
- **Candidate-source share** reports four fractions of proposed edges: requirement-graph traversal, referent match, `constraint-fact` scan, and any other fallback (§5.4). Referent match and the constraint scan are quiet-constraint paths. Neither is a fallback. This share is not recall. Recall's denominator is the independent label set in §5.4, including constraints that were never proposed. A falling requirement-graph share with an empty referent-match share and an empty constraint-scan share, or a run that records `constraint-scan-empty` or `constraint-scan-exhausted`, means recovery did not succeed.
- **Edge density per criterion** — edges among turns attributed to one criterion (§5.3). High density with flat progress is a retry loop, not a discovery.
- **Introduced-harm flag precision** is, of turns flagged `introduced`, the fraction operator review confirms as introduced harm. The name is precision of the flag. It is not accuracy: the definition has no true-negative term. **Introduced-versus-revealed agreement** is, of regression classifications, the fraction whose class matches operator review. Report the two separately.
- **Explanation citation rate** is the fraction of explanations that cite at least one checkable referent (§7.1). A falling fraction means explanations are drifting into narration. The rate does not say the referent supports the claim. **Explanation support rate** is, of cited explanations later reviewed, the fraction whose referent supports the claim. The former name "explanation verifiability" is withdrawn; use these two names.
- **Claim-verdict divergence rate** — how often the actor's intent claim and a non-pending HV verdict disagree (§6.3). The rate is descriptive. Agreement is the expected outcome when the claim is accurate and the work is straightforward, so near-zero divergence is not validator failure and is not evidence of capture. Disagreement is not success either. An uncalibrated target rate would reward false adverse verdicts. Do not alert on agreement, and do not alert on disagreement, until an expected disagreement rate has been calibrated on the labeled set below.
- **Adjudication accuracy** — of HV verdicts later checked by operator review, the fraction that matched the reviewed outcome. This is the metric that can distinguish a competent validator from a captured one. Divergence rate may be compared with it only after that calibration exists.
- **HV adjudication cost** — validator calls, targeted test runs, batched suite runs, and latency, reported separately from §5.2's revalidation cost and not averaged into a per-turn figure that hides batching. Per §6.3 an ordinary turn carries a `pending` verdict and does not launch the suite; the phase-exit suite is one batch charge; a targeted run is an explicit extra. If validator calls or suite runs grow with turn count, adjudication has become the default tier, which is the condition WP §10 risk 6 says to avoid.
- **Revalidation cost** — model judgments per turn and their share of turn latency, against the §5.2 cap. WP §10 risk 6 makes this a first-class concern, not a footnote.

---

## 10. Open questions

A linking run MUST NOT treat open questions 9, 10, 11, and 13 as already decided. Catalog-to-test binding, HV independence, deadlock abandonment, and the per-criterion density cap stay open. A run that assumes any of them is non-conforming for a claim that depends on that answer.

1. **Single-plan attribution is specified; multi-plan attribution is not.** For one plan, §6.3 names the candidate set: test or exercised-code edits, HV-labeled discovery or revealed turns (including turns that never touched the test), and HV-accepted investigation claims. Actor claims alone do not attribute. Referent match and the `constraint-fact` scan (§5.4) can still propose a link for an unattributed quiet constraint, including when the failure does not quote the constraint. Attribution across several slices or plans remains open question 4.
2. **Resolved: introduced-harm is an annotation, not a fourth `projectionState`.** Unpinned turns are `summarized` with the harm marker. Pinned turns stay `expanded` with the same marker (WP §6.2, §7.4). The projector MUST render the marker; ignoring it is non-conforming.
3. **Resolved: the weight-facing reading is the plan vector.** A slice view is a filter of that vector (§6.1). It is not a second progress number and it is not rolled up into another count. Phase-boundary flushes are the `phaseBoundary` flag in WP §9.1, not a second vector.
4. **Multi-plan attribution is unsupported for weight.** One active plan supplies the vector. A turn that also advances another plan does not write a second weight-facing progress number until this question specifies the rule. Until then, packing uses the single plan named on the snapshot.
5. **Resolved: the confidence band is §5.3.** Discretionary admit is `T_admit = 0.80`. Discretionary withdraw is `T_withdraw = 0.60`. The band, the `C = 2` clock, and the behavior after forced invalidation and reconfirmation are specified there. Link admission that has already cleared the band is still a mandatory override in WP §6.1 relative to turn-weight hysteresis. The numbers are provisional only in the sense that an operator may replace them in the run record before the run executes. A run with the cutoffs left unnamed cannot pass or fail. Per-type cutoffs are not used; one band applies to every type in §3.1.
6. **Does revalidation run on seal, on a schedule, or under budget pressure** (compare WP §10 risk 6's mitigation for re-score cost)? Decided in §5.2: reseal and changed evidence void the prior judgment immediately; stale edges are judged once generations since last evaluation reach `S`. The still-open part is whether edges that are not stale, not evidence-changed, and not near the threshold are also judged on a schedule, or only when they later enter the ordering.
7. **Resolved for the overflow case: §3.1 states the final assembly.** The numeric link sub-cap fraction remains operator-adjustable and MUST be fixed before a linking run can pass or fail. When a required source does not fit beside an independently retained target, `invalidates` fails closed and the other types use a dependency-gap marker. Pins are not dropped to pay for the source.
8. **Resolved: an explanation is revised by appending a superseding record.** §5.5 and §7.2 require a new record that names the record it supersedes. The earlier record stays. Effective state is the latest superseding record. In-place rewrite is non-conforming.
9. **Does a passing test prove its criterion, and does the traceability script know the criterion exists?** §6.4's script does not read acceptance catalogs or named test methods. "Covered" in that script means an `FR-` / `TR-` / `TEST-` identifier appears in the mapping or matrix, not that a catalog row names a test, and not that the test is honest. A test bound to `AC-FR-MCP-MEMORY-010-02` can pass while asserting less than the criterion states, and the catalog row can disappear without the script failing. Catalog-to-test validation is an open surface (§6.4), not a closed gate. Test honesty remains the place §6.3's adjudication is load-bearing rather than supplementary, and it does not close the missing catalog check.
10. **What keeps HV independent in practice?** §6.3 requires a validator blind to the actor's account, but a validator sharing the same model, prompt lineage, or projection may reproduce the actor's blind spots and rubber-stamp them. The hostile-review store records model and effort per run, so independence is at least auditable after the fact; whether a *different* model is required, and what that costs, is unsettled.
11. **When does a deadlock become session abandonment?** BDPv4's answer for a session whose trust has broken is to end it rather than retry (§6.4). A metric loop needs a threshold: how many actor-versus-HV rejection cycles on the same criterion, with scope constant, before the correct action is to discard the session rather than record another regression?
12. **Where does a requirement-to-requirement dependency graph come from?** §5.4 can traverse same-criterion pairs, sibling criteria under one requirement, and FR–TR–TEST co-membership with artifacts that exist at the baseline (`docs/plans/*-ac-catalog.json` where a plan has one, `docs/Project/TR-per-FR-Mapping.md`, `docs/Project/Requirements-Matrix.md`). None of those artifacts records a dependency between requirements. Until one does, and until it declares direction and that the edge is a candidate hint rather than an admission, cross-requirement traversal is out of scope. The absence is a missing input, not an empty result the indexer should paper over.
13. **What is the per-criterion turn-density cap?** §5.3 keeps hysteresis and cooldown as the primary churn defence until this cap exists. The open definition is what is counted (turns, edges, or both), the numeric limit, and the projector's behavior at the cap (refuse new link admissions, stub, or hand the criterion to the deadlock rule in open question 11). A note that density "needs a cap" is not the cap.

---

## 11. Non-claims

Stated explicitly, because the underlying proposition — that retrospective linking improves outcomes — is attractive enough to be assumed rather than tested.

- **No efficacy claim.** That linked context produces more correct results is a **hypothesis**, not a finding. Nothing here has been built or measured. WP Principle 8 bars presenting it as a benefit until §9's metrics say so.
- **No token-savings claim.** Nothing here is offered as reducing token cost. §5.2 says the opposite: revalidation adds model calls, and the cost must be bounded and reported.
- **No claim that precision is achievable.** §4 hypothesizes that false admissions are costly. §9 predeclares the admission-precision gate. Whether that gate is reachable is unknown, and a negative result is a legitimate outcome. Failing the gate leaves admission off.
- **No scalar-impossibility theorem.** §2 does not claim that a scalar output makes the judge unable to notice joint relevance. WP §5.2 already shows the judge several stubs at once. The claim is narrower: the stored scalar does not persist the pair and does not pack both turns.
- **No universal false-link cutoff.** The withdrawn "60% is worse than no index" sentence is not a result. The adoption rule is the §9 gate.
- **No claim of code readiness.** §8's references are a snapshot of one baseline commit, read for shape rather than verified by building anything.
- **No claim that self-reported signals are trustworthy.** §6.3 removes the actor from adjudication and §7.1 constrains both sides to checkable referents, precisely because the unconstrained versions are not trustworthy. Those constraints are reasoned, not demonstrated.
- **No claim that Hostile Validation is available, cheap, or complete.** §6.3 proposes HV as a runtime adjudicator; that is a design proposal. The existing document-level Perplexity HV remains blocked on an API key after box reseed and has **not** been run. Per the standing rule in the README and IMPL §9, HV must never be described as done.
- **No claim about undisclosed search internals.** §4.1 cites patents, Google's own published posts and documentation, a court exhibit, and a peer-reviewed paper. It does not claim to describe how any production ranking system currently works, and the mapping from web spam to agent context is an analogy — §4.1 states where it breaks rather than where it holds. No claim is made that any of this prior art was designed for, or validates, the design in this addendum.
- **No claim about how BDPv4 is practiced.** §6 and §6.4 quote `docs/Development-Process-draft-v4.md`, the plan artifacts in `docs/plans/`, and `scripts/Validate-RequirementsTraceability.ps1` as written. Whether those gates are applied consistently, whether the traceability script runs in CI, and whether the acceptance catalogs stay current were not checked. The process is cited as documented intent, not as observed practice.
- **No claim that scope and progress can be read automatically today.** §6.1 derives them from acceptance-criteria state, which assumes a machine-readable catalog bound to test results. `docs/plans/mcp-memory-002-ac-catalog.json` shows the shape exists for one plan; no such binding was verified across plans, and nothing computes these values.
- **No claim that an adversarial validator is correct.** §6.3 treats HV verdicts as evidence with their own error modes, including false accusation, and open question 10 records that a validator sharing the actor's model or lineage may simply reproduce its blind spots.
- **No claim that retraction rate measures edge precision.** §5.5 and §9 separate churn (retractions, flips) from precision, which needs labels the indexer did not assign.
- **No claim that traceability validation closes criteria gaming.** `scripts/Validate-RequirementsTraceability.ps1` checks identifier presence in the mapping and matrix. It does not read acceptance catalogs or bound tests. That surface is open (§6.4, open question 9).
- **No claim that a requirement-to-requirement dependency graph exists.** §5.4 names the traversals the baseline artifacts support and the cross-requirement traversal they do not (open question 12).
- **No claim that actor/HV agreement is capture, or that disagreement is success.** §9 refuses both readings until adjudication accuracy has calibrated an expected disagreement rate.
- **No claim that per-turn progress readings are free.** §6.3's ordinary turn carries a pending verdict. The phase-exit suite is a batch charge and does not attribute itself to turns.
- **No claim that work per generation is O(K), that revalidation cost is bounded by `J` rather than by graph size, that session cheap work is O(turns times (D + K_crit + K_ref)), or that session cheap work is O(G_session times (T_new + K_gen + J + F_max) + E_events).** Those sentences are withdrawn (§5.4). The v0.1.9 formula omitted posting-list reads, did not forbid a walk of E_live, and did not schedule first judgments. The v0.1.10 formula is also withdrawn: it capped each list's length and did not cap how many lists a turn opens, and it omitted `E_events` from the displayed expression. The bound that holds is the procedure stated in §5.4: O(G_session times (T_new + P_open times L + K_attempt + J + F_max) + E_events). `P_open` caps list openings, `K_attempt` caps attempted pairs including rejected and duplicate hits, `inspectionCursor` keeps the unopened keys, `E_events` counts O(1) watermark bumps, and `J_first` with FIFO service is unchanged. An implementation that scans E_live, or that opens every referent list on a turn in one generation, is non-conforming, and that bound does not apply to it.

---

## 12. Document control

| Version | Date | Change |
|---|---|---|
| v0.1.12 | 2026-09-25 | Astra re-review (round 6) against tip `f5bfa051`. See the round-6 finding map below. That map is claimed remediation for P2-07, pending an independent Astra review. It is not an AGREE. Companions: WP v0.1.15, IMPL v0.1.10 |
| v0.1.11 | 2026-09-25 | Astra re-review (round 5) against tip `30aa90e5`. See the round-5 finding map below. That map was claimed remediation. The round-6 receipt verified P1-06, P1-07, and P2-09 from it, and left P2-07 open. Companions at the time: WP v0.1.14, IMPL v0.1.9 |
| v0.1.10 | 2026-09-25 | Astra re-review (round 4) against tip `710addcb`. See the round-4 finding map below. That map was claimed remediation. The round-5 receipt verified P1-04, P1-05, P1-10, P1-11, P1-12, P2-01, and P2-04 from it, and left P1-06, P1-07, P2-07, and P2-09 open. Companions at the time: WP v0.1.13, IMPL v0.1.8 |
| v0.1.9 | 2026-09-25 | Astra re-review (round 3) against tip `3291fb34`. See the round-3 finding map below. Those rows were claimed remediations. The round-4 receipt did not verify them as full closures, except P1-03, P1-08, and P2-06. Companions at the time: WP v0.1.12, IMPL v0.1.7 |
| v0.1.8 | 2026-09-25 | Astra re-review (round 2) against tip `941cfa99`. See the round-2 finding map below. That map records subdefect repairs. It is not a full closure of P1-03, P1-04, P1-05, P1-06, P1-07, P1-08, P1-10, P1-11, P1-12, P2-01, P2-06, or P2-07. Companions at the time: WP v0.1.11, IMPL v0.1.6 |
| v0.1.7 | 2026-09-25 | Hostile review remediation. See the finding map below. Pins, introduced-harm, hysteresis, cooldown, progress, and persistence now match WP v0.1.10 |
| v0.1.6 | 2026-09-25 | Codex P1 on PR #58. §3.1 `invalidates` admission leaves a pinned target expanded. An operator, system, or acceptance pin stays verbatim under WP Principle 5 and WP §6 step 3; the invalidation marker is attached separately, and budget pressure still fails closed rather than dropping pinned content. An unpinned obsolete target that is already present may still be forced to a marked stub. README index and status synchronized to v0.1.6 |
| v0.1.5 | 2026-09-25 | Codex review on merged PR #56. Design claims corrected. P1: traversal, link admission, and projection reads require both endpoints non-deleted (§8); retraction rate is churn, and precision needs independent true/false labels (§5.5, §9); catalog-to-test coverage left open because `Validate-RequirementsTraceability.ps1` does not read acceptance catalogs (§6.3, §6.4, open question 9); edge judgments bind to seal sequences and reseal invalidates (§5.2); stubs keep residual payload facts as well as the outcome record (§7.3, §7.5). P2: §1 retracts the claim that omission hides turns from the scorer — WP §6 loads every turn and WP §5.2 scores stubs; the remaining hole is joint value and facts the stub dropped. Per-turn HV verdicts are pending, batched at a checkpoint, or targeted, and are not a free reading of the phase-exit suite (§6.3). Admission direction is per edge type (§3.1). Shared criteria generate candidates only; type and direction still need evidence (§5.4). Cross-requirement dependency traversal acknowledged missing (open question 12). Hysteresis and cooldown returned to the primary churn defence until a per-criterion density cap is defined (§5.3, open question 13). Actor/HV agreement is not treated as validator failure (§6.3, §9). README index version synchronized to this document |
| v0.1.4 | 2026-09-20 | Operator correction: the bounded acceptance-criteria set of a BDPv4 phase is the primary defence against edge churn, so §5.3's thrashing risk was overstated — hysteresis and cooldown are refinements over a finite, slowly-moving anchor rather than the only protection. Follows the same anchor into new §5.4: candidate generation is a traversal of the requirement graph, which drops the quadratic-pairing and embedding-index problem, makes edge types declared rather than inferred, and gives the §4 task-progress rule a concrete form. Records that bounding traversal by phase would reopen §1's hole, so lineage rather than time is the bound; and that phase-boundary turnover is a scheduled flush, not thrash. Old §5.4 (append-only lifecycle) renumbered to §5.5 with references updated. Open question 1 replaced — candidate generation is no longer the load-bearing unknown; turn-to-criterion attribution is. Three metrics added. Superseded in part by v0.1.5: the churn-priority claim and the "edge types become declared" claim were wrong, and are corrected there rather than rewritten out of this row |
| v0.1.3 | 2026-09-20 | Adds §4.1, prior art on adversarial link graphs, in answer to an operator question about public attribution: reasonable-surfer per-link weighting, neutralize-not-penalize link spam handling, link attributes as hints so information is not lost, and TrustRank's expert-evaluated seed set as a counter-bias — the last being the one result that transfers. Records the unflattering finding that the declared link graph was demoted in favour of recorded user reaction, a signal with no analogue in a single session, and the two disanalogies that follow: opacity is unavailable when the actor reads this document, and the misaligned party is inside the system. Corresponding non-claim added |
| v0.1.2 | 2026-09-20 | Operator correction: **the plan is the goal, not the turn**, and planning — not adjudication — closes the criteria surface. §6.1 re-scoped to the plan and given real units from BDPv4's requirement/acceptance-criteria artifacts; new §6.4 on criteria preceding implementation, machine-checked traceability, and the narrower gaming mode BDPv4 already documents (invalidating a blocking test); §6.3's cost tier corrected — the base reading is a test run, not a model call; descoping assigned to stakeholders per BDPv4; new §7.1.1 routing explanations into requirement refinement. §6.2's introduced-versus-revealed argument **retracted as novel** and re-credited to BDPv4 "Resolving Defective Requirements." Open questions 3, 4, 9, 10 and 11 rewritten — criteria authorship is no longer open; test honesty is what survives |
| v0.1.1 | 2026-09-20 | Operator correction before first review: progress, regression and explanation records are outputs of **Hostile Validation**, not of the acting agent. §6.3 rewritten around actor intent claim versus HV verdict, with claim-verdict divergence as the harm signal; §7 and §7.3 reworked so the outcome record pairs both halves; the blindness constraint from §5.1 generalized into a single rule covering both link revalidation and progress adjudication. Records where gaming relocates to (criteria authorship) and adds open questions 9–11 on criteria provenance, validator independence, and actor/HV deadlock |
| v0.1.0 | 2026-09-20 | Initial draft. Captures four operator proposals from the 2026-09-20 design session: retrospective link index, continuous revalidation, per-goal scope and progress, and result-with-explanation records. Adds the re-admit circularity argument (§1), the joint-value representational limit (§2), the self-confirmation constraint on revalidation (§5.1), the introduced-versus-revealed correction to the harmful-turn rule (§6.2), and the traversal hazard that IMPL §3.2.3 creates for an unregistered edge table (§8) |

### Hostile review remediation (round 6 / Astra re-review, v0.1.12)

Full-document gpt-6-astra xhigh re-review of tip `f5bfa051`. Verdict on that tip was DISAGREE (93/100). P0=0, P1=0, P2=1, P3=0. This section is **claimed remediation** for P2-07, pending an independent Astra review. It is not an AGREE and it is not a verified closure. WP is `whitepaper-weighted-context-projection-v0.1.md` at v0.1.15. ADD is this document at v0.1.12. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md` at v0.1.10.

**Verified by the round-6 receipt, and not reopened here:** P1-01, P1-02, P1-03, P1-04, P1-05, P1-06, P1-07, P1-08, P1-09, P1-10, P1-11, P1-12, P2-01, P2-02, P2-03, P2-04, P2-05, P2-06, P2-09, P3-01. Pin rule unchanged. P2-02 denominators stay the previous active set and the previous variable tail.

**Claimed in this round because the round-6 receipt left it open:** P2-07.

- **P2-07 (claimed remediation).** WP §6.1, WP §9.1 metric 3, and ADD §9. A `rendered-change` event is required whenever a sent segment changes, including when `projectionState` and tail membership stay the same. The event binds `previousRenderId` and `currentRenderId` and attributes each removed span. A discretionary summary rewrite is `summary-rewrite` and counts in metric 3. A mandatory marker removal on the same turn is a separate span and stays in `mandatory-token-count`. Metric 2 still counts only a `projectionState` change whose cause is `score`. Unchanged-state trace: 90 of 100 tail tokens disappear, metric 3 is 0.90 and fails, metric 2 numerator is 0. Mixed-span trace: 30 discretionary plus 20 mandatory on one retained turn, metric 3 numerator is 30.

### Hostile review remediation (round 5 / Astra re-review, v0.1.11)

Full-document gpt-6-astra xhigh re-review of tip `30aa90e5`. Verdict on that tip was DISAGREE (95/100). P0=0, P1=2, P2=2, P3=0. This section was **claimed remediation** when written. **Status after the round-6 receipt:** P1-06, P1-07, and P2-09 from this list are verified. P2-07 stayed open. The current claimed text for P2-07 is the round-6 map. WP is `whitepaper-weighted-context-projection-v0.1.md` at v0.1.14. ADD is this document at v0.1.11. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md` at v0.1.9.

**Verified by the round-5 receipt, and not reopened here:** P1-01, P1-02, P1-03, P1-04, P1-05, P1-08, P1-09, P1-10, P1-11, P1-12, P2-01, P2-02, P2-03, P2-04, P2-05, P2-06, P3-01. The must-hold set is P1-01, P1-02, P1-03, P1-08, P1-09, P2-02, P2-03, P2-05, P2-06, and P3-01. Pin rule unchanged: pinned targets stay expanded; harm and invalidation are markers; unpinned targets may stub. P2-01's verified closure is this round-5 receipt. The round-3 bullet was a claimed repair. It did not close P2-01, and the round-4 receipt still left P2-01 open.

**Claimed in this round because the round-5 receipt left them open:** P1-06, P1-07, P2-07, P2-09.

- **P1-06 (claimed remediation).** ADD §5.4. `failureEvidenceRevision` is (`evidenceWatermark`, `claimSetVersion`, `rawErrorHash`). Judgments and coverage bind to that tuple. A raw-error change bumps `claimSetVersion` and the watermark. Either change invalidates prior rejections and coverage stops on the same turn. The cursor restarts at the oldest ledger entry. Trace: C rejected at version 1 is proposed again at version 2 without a new failure turn.
- **P1-07 (claimed remediation).** ADD §5.4 and §11. `P_open` caps list openings. `K_attempt` caps attempted pairs, including duplicates and rejected hits. `inspectionCursor` keeps unopened keys. Session cheap work is O(G_session times (T_new + P_open times L + K_attempt + J + F_max) + E_events). The v0.1.10 formula is withdrawn. `J_first` and FIFO service stay. Trace: 1000 referents, near-zero emission, bounded openings.
- **P2-07 (claimed remediation).** WP §6.1, WP §9.1 metric 3, and ADD §9. Each change stores `turnCause` and `originCause`. State-flip still counts only `turnCause` `score`, and its denominator stays the previous active set. Token replacement counts absent previous-tail tokens whose `originCause` is `score` or `edge-score`. Mandatory withdrawals, pins, and phase boundaries stay outside that numerator. Trace: a discretionary edge withdrawal removes 90 of 100 tail tokens, the edge-flip rate passes, and the token gate fails.
- **P2-09 (claimed remediation of the ledger).** This section. The round-2 sentence no longer says P2-01 stayed open until the round-3 bullet. P2-01 remained open through the round-4 receipt. The round-5 receipt is the review that closed it. There is still no P2-08.

### Hostile review remediation (round 4 / Astra re-review, v0.1.10)

Full-document gpt-6-astra xhigh re-review of tip `710addcb`. Verdict on that tip was DISAGREE (96/100). P0=0, P1=7, P2=4, P3=0. This section was **claimed remediation** at the time it was written. **Status after the round-5 receipt:** verified from this list are P1-04, P1-05, P1-10, P1-11, P1-12, P2-01, and P2-04. P1-06, P1-07, P2-07, and P2-09 stayed open. The round-6 receipt later verified P1-06, P1-07, and P2-09. P2-07's current claimed text is the round-6 map. Do not treat a round-4 bullet as verified unless this sentence names it. WP is `whitepaper-weighted-context-projection-v0.1.md` at v0.1.13. ADD is this document at v0.1.10. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md` at v0.1.8.

**Verified closed by the round-4 receipt, and not reopened here:** P1-01, P1-02, P1-03, P1-08, P1-09, P2-02, P2-03, P2-05, P2-06, P3-01. Pin rule unchanged: pinned targets stay expanded; harm and invalidation are markers; unpinned targets may stub.

**Reopened by that receipt:** P2-04. Withdrawing the 60% sentence was not enough. Weighting errors still described omission only.

**Claimed again in this round, because the round-3 text was not verified:** P1-04, P1-05, P1-06, P1-07, P1-10, P1-11, P1-12, P2-01, P2-07, P2-09.

- **P1-04 (claimed remediation).** ADD §6.1. Satisfaction binds `observationSeq` as well as the five artifact versions. A newer same-binding `fail` or `skipped` sets current satisfaction to `disputed` before the next HV record. The older `passing` row stays readable and is not current. Acceptance case: same-binding pass-then-fail.
- **P1-05 (claimed remediation).** ADD §6.1 and §6.5. The turn reading keeps per-criterion `gain`, `loss-introduced`, `loss-revealed`, and `none`. A gain does not cancel an introduced-harm warning or replace the ceiling with the floor. Acceptance case: one plan, A gains, B regresses, scope flat, passing count flat, ceiling and marker still apply.
- **P1-06 (claimed remediation).** ADD §5.4. `failureId`, `claimSetVersion`, and `claimSetStatus` (`empty`, `unclassified`, `partial`, `complete`) are defined. An empty or unclassified set is not complete. Vacuous coverage is not a stop. HV stops an empty set only with `adjudicated-empty`. A new raw-error hash bumps the version and reopens the scan. Recovery case: raw error with no claim id.
- **P1-07 (claimed remediation).** ADD §5.2 and §5.4. The v0.1.9 formula O(G_session times (T_new + K_gen + J + F_max) + E_events) is withdrawn. Posting lists including the load-bearing list are capped at L. The generation does not walk E_live. `J_first` is reserved for never-judged candidates. FIFO position n is judged within n generations under sustained arrivals. Session cheap work is O(G_session times (T_new times L + K_gen + J + F_max)). An E_live scan is non-conforming and the bound does not apply to it. `J_void` and `J_stale` are unchanged.
- **P1-10 (claimed remediation).** ADD §7.4, WP §6.2, and WP §6.3. A replacement must state the prohibited approach or obsolete result, harm or invalidation, a checkable referent, and not-the-next-action, in the sent text. Id-only does not retire. Transferred protection holds only while that replacement is present and adequate in the sent assembly. Removal reactivates the individual marker before send. Pins stay expanded. Active and reactivated markers stay spill-protected.
- **P1-11 (claimed remediation).** ADD §3.1 and WP §6.1 step 3. Depth truncation uses the same rendering as no-fit: dependency-gap marker, or fail-closed when the truncated edge is `invalidates`. An independently retained endpoint, including one admitted as a source, is not sent with a further required source silently absent. Acceptance case: depth-three chain with enough budget that depth alone drops S3.
- **P1-12 (claimed remediation).** WP §6.1 and ADD §8, restated in IMPL §5.1, Option C, §8.1, and open question 7. `sendFence` covers the final read and transport handoff. Exactly one of handoff or mutating commit wins. The required race injects a tombstone after the final read is attempted and before handoff. The published call must not contain that endpoint.
- **P2-01 (claimed remediation).** ADD §1, ADD §2, ADD §11, and WP §5.2. The categorical denial that re-admit does not fire for joint value is replaced by a lack-of-guarantee claim. Missing inputs, contextual inference, persistent relation storage, and dependency-aware packing stay separate.
- **P2-04 (reopened, claimed remediation).** ADD §4 and §11. Omission and misleading admission are both weighting errors. Causal endorsement of links stays a hypothesis against the without-admission replay. False weights are not called safe or internally honest. The 60% sentence stays withdrawn.
- **P2-07 (claimed remediation).** WP §6.1, WP §9.1, and ADD §9. Turn-layer and edge-layer causes are named, including link admission, withdrawal, degradation, and reconfirmation, and each maps into the metric that may count it. Only `score` and `edge-score` are discretionary in their numerators. Zero counted generations record `empty-aggregate` and the gate passes. The projector sets `phaseBoundary` from the plan phase id. Mixed-cause numeric results are in §9.
- **P2-09 (claimed remediation of the ledger, not a verified closure of the other ids).** This section. Corrected identities stay. Round-3 bullets for the ids listed above were claimed remediations, not verified closures. This round does not call them fully closed. There is still no P2-08.

### Hostile review remediation (round 3 / Astra re-review, v0.1.9)

Full-document gpt-6-astra xhigh re-review of tip `3291fb34`. Verdict on that tip was DISAGREE (97/100). P0=0, P1=9, P2=4, P3=0. P1-01, P1-02, and P1-09 stayed closed and are not reopened. The round-2 rows for the ids below were subdefect repairs, not full closures. Round-2 P2 ids in the receipt were P2-01, P2-06, and P2-07. There was no P2-08. **Status after the round-4 receipt:** verified closed from this list are P1-03, P1-08, and P2-06. The other bullets in this list were not verified. Do not read them as full closures. The round-5 map is the current claimed text for ids that receipt left open. Round-5 verified closures are named in the round-5 map and are not retroactive to this list. WP is `whitepaper-weighted-context-projection-v0.1.md`. ADD is this document. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md`.

- **P1-03.** ADD §5.2. A confirming judgment stores a `claimFamily`. A later `contradicts` turn bumps `evidenceWatermark` for that family even when it is outside the citation set and even when it does not edit either endpoint. The edge is suspended, admission bit clear, until re-judged. The acceptance trace is turns A and B with a confirmed `explains` edge, then turn C disproving the approach: the next send after C does not carry that admission.
- **P1-04.** ADD §6.1 and §6.3. Test observation and HV satisfaction are separate records, each bound to code, test, criterion, catalog, and environment versions. HV outranks the test. A later pass on the same binding does not erase an adverse HV judgment. A binding change marks un-readjudicated satisfaction `disputed`. Acceptance cases: passing-but-inadequate test; targeted run after a shared dependency changes.
- **P1-05.** ADD §6.5 and WP §5.1. Floors and ceilings apply only while the contribution is active, and they still do not stack across superseding verdicts. Completion releases the floor by recomputation without a false corrective verdict. The audit credit stays. Acceptance traces cover verdict correction and completion-driven relevance loss.
- **P1-06.** ADD §5.4 and §9. The ledger retains every `constraint-fact`, not the newest four and not only the hot list of `L`. A cursor proposes oldest-unjudged first. One `explains` or `constrains` edge does not suppress the scan. `constraint-scan-empty` and `constraint-scan-exhausted` are not success. Recall uses an independent label denominator. Recovery cases: old, paraphrased, summary-lost, unattributed, crowded-out.
- **P1-07.** ADD §5.4 and WP §6.1. Withdrew the session bound O(turns times (D + K_crit + K_ref)) and the O(D) shared-event enqueue. Bound claimed in that pass, and withdrawn in round 4: `T_new`, `K_gen`, watermark fan-out with `F_max`, overflow cursor rather than a silent drop or a second queue, and `G_session` counting every tool-loop call. The round-4 formula was withdrawn in round 5. The current claimed formula is the round-5 bullet. Acceptance traces: multi-call turn, high fan-out evidence change, sustained arrival above capacity.
- **P1-08.** ADD §9. Live admission stays off. Shadow compatibility is named predicted usefulness and cannot enable admission. General enablement requires isolated with/without replay, independent adjudication, frozen workload and config, recovery recall ≥ 0.80 on the §5.4 label set, and requalification after a material generator, judge, or config change.
- **P1-10.** WP §6.2, WP §6.3, and ADD §7.4. Only active warnings are mandatory. Retirement, consolidation at `W_max`, and standing-memory transfer keep the audit verdict and drop the projection obligation. Spill protection remains for active markers. Pins stay expanded. Acceptance case: resolved regressions that exceed `B` as raw markers still project, with one still-active marker retained.
- **P1-11.** ADD §3.1 and WP §6.1 step 3. The invariant is the final assembly, including independently pinned or scored endpoints. `invalidates` that cannot fit fails closed. Other types on an independently retained target use a dependency-gap marker rather than a silent omission. Depth `Dep = 2`, cycle stop, dedup, and one sub-cap charge per turn are specified.
- **P1-12.** WP §6.1 and ADD §8. The frozen snapshot includes the tombstone set and the rendered inputs. The commit point claimed here was send. The round-4 receipt kept the after-read-before-handoff race open. The current claimed text is `sendFence` in the round-4 bullet. Tombstone, reseal, evidence change, or superseding verdict before send aborts and retries. After send, those events wait for the next generation. Each tool-loop call is its own generation. Traces: tombstone-before-send, reseal-before-send, concurrent supersession, interrupted generation.
- **P2-01.** ADD §1, ADD §2, ADD §11, and WP §5.2. Missing inputs, contextual inference, persistent relations, and dependency-aware packing are separate. Independent scalar outputs do not imply the judge cannot notice joint relevance in a multi-stub prompt. The without-admission replay is the context-conditioned baseline.
- **P2-06.** ADD §5.3, ADD §10 question 5, and WP §6.1. Executable before use: `T_admit = 0.80`, `T_withdraw = 0.60`, cooldown start and reset, mandatory reconfirmation after forced invalidation, and `Δweight` against `weightAtLastStateTransition`. Capacity-only expansion remains prohibited. Traces: gradual 0.04 drift, and invalidation plus reconfirmation.
- **P2-07.** WP §9.1 metrics 2 and 3, and ADD §9. Empty denominators pass with an explicit empty record. Mandatory transitions are reported and excluded from the numerator. Phase-boundary exclusion requires `phaseBoundary` plus `phaseId` on the snapshot. Edge flips count one admission-bit change per edge per published snapshot. Two evaluators of one trace use those rules and no others.
- **P2-09 (ledger correction only; not a verified closure of the other ids).** This section, the v0.1.8 map below, and the v0.1.7 map. Stable ids are preserved. Round-2 receipt P2s were P2-01, P2-06, and P2-07, and P2-01 stayed open. The v0.1.8 labels P2-06, P2-07, and P2-08 were misidentifications of subdefects (capacity-only packing, spill-safe markers, shadow bootstrap). There is no P2-08. Subdefect repairs stay in the round-2 map and are marked as such. The round-4 receipt found that calling the other round-3 bullets full closures overstated the ledger. As of that receipt those ids were still open. The round-5 receipt later verified P1-04, P1-05, P1-10, P1-11, P1-12, P2-01, and P2-04. It left P1-06, P1-07, P2-07, and P2-09 open. P2-01's closure is the round-5 receipt, not the round-3 bullet.

### Hostile review remediation (round 2 / Astra re-review, v0.1.8)

Full-document gpt-6-astra xhigh re-review of tip `941cfa99`. Verdict on that tip was DISAGREE. P1-01, P1-02, and P1-09 stayed closed and are not reopened. The bullets in this section are **subdefect repairs**. Round 6 is the current claimed text for P2-07. The round-6 receipt verified P1-06, P1-07, and P2-09. Round-5 verified closures are not rewritten back onto these rows. They are not full closures of P1-03, P1-04, P1-05, P1-06, P1-07, P1-08, P1-10, P1-11, or P1-12. The round-2 receipt's P2 ids were P2-01 (remained open through the round-4 receipt; the round-3 bullet was a claimed repair, not a verified closure; the round-5 receipt is the review that closed P2-01, and that closure is not retroactive), P2-06 (edge hysteresis and cooldown), and P2-07 (stability metric boundaries; the token-churn acceptance rule stayed open after round 5 for same-state rewrites, and the round-6 map is the current claimed text). This section previously mislabeled three different subdefects as P2-06, P2-07, and P2-08. That labeling was wrong. There was no P2-08. WP is `whitepaper-weighted-context-projection-v0.1.md`. ADD is this document. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md`.

- **P1-03 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §5.2. Judgment cap `J = 8` is split: `J_void = 4` for seal and evidence voids (items 2 and 3), `J_stale = 2` reserved for the oldest stale edges (item 6; items 1, 4, and 5 MUST NOT consume those slots), remainder 2 for load-bearing, near-threshold, and weight-moved edges. `J_stale < 1` or `J < J_void + J_stale` is non-conforming. At `S + S_starve` (`S = 10`, `S_starve = 10`) a stale edge is promoted ahead of items 4 and 5. Changed evidence and staleness still force judgment. The round-1 skip-rule fix stays.
- **P1-04 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §6.1. Authoritative progress is a per-criterion state vector (`passing`, `failing`, `skipped`, `untested`, `blocked-no-test`), not a single integer. Scope counts every in-scope criterion. Progress counts `passing` only; `skipped` is not passing. A tier-2 run updates only criteria the suite executed. A tier-3 run updates only criteria it executed and MUST NOT replace the vector with a partial count or mark an unrun criterion passing or failing. A criterion id appears once. Effective state is the latest append-only revision. Introduced versus revealed classifies a downward revision of this same vector. WP §5.1 still points here.
- **P1-05 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §6.5. `scorerWeight` is WP §5.2. `effectiveWeight` applies only the latest superseding verdict. Floors and ceilings do not stack. Progress-up and blocker discovery use `max(scorerWeight, 0.60)`. Introduced harm uses `min(scorerWeight, 0.20)` plus WP §6.2. A revealed regression on the revealing turn does not take the harm ceiling. A superseding `revealed` or no-delta verdict removes the 0.20 ceiling and the harm annotation on the next snapshot. Pins stay expanded. An unpinned turn is no longer forced to the harm stub. Pending and actor claims do not revise weight.
- **P1-06 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §5.4 and ADD §1. Exact referent match remains, and it is not sufficient when the failure does not quote the constraint. On append, operator constraints, environment conditions, and "never" commitments MUST be marked `constraint-fact` even with no criterion id. If a load-bearing turn has no effective `explains` or `constrains` edge, propose up to `K_constraint = 4` newest constraint-fact turns, counted inside `K_ref = 8`. A missing mark plus no shared referent records `constraint-scan-empty`, which is not success. Candidates only; type and direction still need out-of-band judgment. Not `similar_to`. §9 reports the constraint scan as its own candidate-source fraction.
- **P1-07 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §5.4 and ADD §11. The sentences "work per generation is O(K)" and "revalidation cost is bounded by `J` rather than by graph size" are withdrawn. Bounds claimed in that pass, and withdrawn where the round-3 map says so: emission `K_crit = 8` plus `K_ref = 8`; posting lists `L = 16`; degree `D = 32`; model judgments `J` and `J * T_j` (`T_j = 4096`); session cheap work stated then as O(turns times (D + K_crit + K_ref)). That session sentence does not hold. Use the round-5 bound in §5.4. The round-3 and round-4 formulas are withdrawn.
- **P1-08 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §9 and ADD §4. Live admission stays off until the gate passes. The sample is a shadow population: would-be admissions are logged and do not enter the model-facing projection, do not charge the link sub-cap, and are not shown to the actor. Provisional bar: at least 50 proposed and at least 30 shadow-admitted edges; admission precision ≥ 0.90 on the shadow-admitted population; shadow usefulness ≥ 0.80, labeled against the sealed session rather than by a live model that saw the edge. No live trial before the gate. After the bar, live admission MAY turn on. Shadow logging stays.
- **P1-10 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** WP §6.3, restated in IMPL §5.1, Option C, and §8.1. Spill unmarked unpinned content first. Shrink an unpinned harm or invalidation stub to the marker; do not omit the marker. The mandatory fail-closed set includes the prefix, standing memories, pins with their markers, the live result, and every required harm or invalidation marker. Pins stay expanded.
- **P1-11 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** ADD §3.1, applied in WP §6.1 step 3. Same-generation co-presence. For `explains`, `constrains`, and `depends_on`, the load-bearing target and the admitted source both stay, or the edge admits neither; do not demote the target to pay for the source. `shares_root_cause` is both or neither. `invalidates` keeps the source plus the target's required form (expanded pin or unpinned marked stub); if they do not fit, fail closed. Do not drop the pin or the marker.
- **P1-12 (subdefect only; not a verified closure except where the round-4 map says the receipt closed it).** WP §6.1 generation snapshot, applied per call in IMPL Option C and §8.1. Generation G freezes seals, effective edges, scorer weights, pins, the progress vector, the turn set, and live content before judgments, admission, or packing. Writes during G become visible at G+1. A reseal, evidence change, or superseding verdict after the freeze aborts G. Each tool-loop model call is its own generation.
- **Subdefect, not P2-06.** WP §6 step 5 and WP §6.1 step 6. Packing MUST NOT expand an omitted or summarized turn only because residual capacity grew. This repaired a packing hole. It did not define the edge confidence band or the `Δweight` baseline. The receipt id P2-06 stays the edge-hysteresis contract. The round-4 receipt verified that closure.
- **Subdefect, not P2-07.** WP §6.3 spill keeps active harm and invalidation markers. This repaired marker loss during spill. It did not define stability-metric boundaries. The receipt id P2-07 stays the metric contract. The round-3 and round-4 bullets were not verified for the token-churn rule. The round-6 bullet is the current claimed text. The round-5 bullet closed the edge-withdrawal case and left same-state summary rewrites open.
- **Subdefect, not a P2-08.** ADD §9 shadow assemblies let the admission-precision sample exist while live admission is off. The round-2 receipt had no P2-08. Predicted usefulness was still being treated as an observed outcome; the round-3 P1-08 bullet is the full gate.

### Hostile review remediation (v0.1.7)

Full-document Codex review against tip `acd1453c`. Each finding is closed in the section named here only where a later round does not say otherwise. Round 2 and round 3 supersede the rows for P1-03, P1-04, P1-05, P1-06, P1-07, and P1-08. P1-01, P1-02, P1-09, P2-02, P2-03, P2-05, and P3-01 stay closed as written here. P2-04's withdrawal of the 60% sentence stays, and round 4 reopens the omission-only remainder. P2-01 does not stay closed here: the round-2 receipt kept it open, and later maps are claimed remediations rather than verified closures. WP is `whitepaper-weighted-context-projection-v0.1.md`. ADD is this document. IMPL is `proposed-implementation-weighted-context-projection-v0.1.md`.

- **P1-01.** WP §6.1. One ordered transition. Mandatory overrides (pins, budget demotion, invalidation, introduced-harm) apply when weight is unchanged and during cooldown. Discretionary score changes wait on `|Δweight| ≥ 0.05` and cooldown `C = 2`. Link admission is step 3, before discretionary scores and before packing. ADD §5.3 uses the same `C` for discretionary edge flips.
- **P1-02.** WP §6.2 and ADD §7.4, same rule. Pinned introduced-harm stays expanded with a harm marker. Unpinned introduced-harm is a marked stub. No fourth `projectionState`. ADD open question 2 is closed. The pin invariant is unchanged.
- **P1-03 (superseded by the round-2 row).** ADD §5 and §5.2. Changed cited evidence voids admission even when seal, weight, projection state, and goal attribution are unchanged. Stale edges (`S = 10`) stay in the judgment order. The skip rule cannot drop either queue. Round 2 adds reserved slots and starvation promotion; this row is not the current bound.
- **P1-04 (superseded by the round-2 row).** ADD §6.1, with WP §5.1 pointing at it. Round 1 named progress as the latest passing-test count. Round 2 replaces that with the per-criterion state vector. This row is not the current definition.
- **P1-05 (superseded by the round-2 row).** ADD §6.3 includes discovery and revealed turns that never touched the test. Round 1 named floors 0.60 and a ceiling 0.20. Round 2 replaces that with non-stacking `effectiveWeight` and reversal on a superseding verdict. This row is not the current weight rule.
- **P1-06 (superseded by the round-2 row).** ADD §5.4 referent match. Round 1 stopped at an exact shared string. Round 2 adds the `constraint-fact` scan for failures that do not quote the constraint. This row is not the current recovery rule.
- **P1-07 (superseded by the round-2 row).** ADD §5.4. All-pairs inside a criterion is withdrawn. Round 1 also said session work is O(turns times (K_crit + K_ref)) and that `J` bounds revalidation cost. Those cost sentences are withdrawn. Use the round-5 bound in §5.4. The round-3 and round-4 formulas are withdrawn.
- **P1-08 (superseded by the round-2 row).** ADD §9. Proposal precision and admission precision are different populations. Round 1 required admitted edges while live admission was off. Round 2 collects that sample on the shadow population. This row is not the current gate.
- **P1-09.** WP §6.3. Every issued model call, including tool-loop continuations, charges live content, spills unpinned traces, then fails closed. IMPL §5.1, §6 Option C, and §8.1 match that rule.
- **P2-01 (not closed here).** ADD §2. Round 1 limited the claim to WP's independent per-turn weight and rejected a general scalar-impossibility theorem. The round-2 receipt kept P2-01 open because that text still treated independent outputs as independent inference. Later maps claim a narrower lack-of-guarantee sentence. That claim is not an independently verified closure.
- **P2-02.** WP §9.1 metrics 2 and 3. State-flip denominator is the previous active set. Active-context token replacement uses the previous variable tail and a ≤ 0.25 bar. ADD §5.3 and §9 cite both.
- **P2-03.** ADD §9. "Harm detection accuracy" is now introduced-harm flag precision plus introduced-versus-revealed agreement. "Explanation verifiability" is now explanation citation rate plus explanation support rate.
- **P2-04 (60% withdrawal stays; omission-only remainder reopened in round 4).** ADD §4 and §11. The 60% sentence is withdrawn. False-admission cost is a hypothesis tested by the §9 gate. Round 1 did not treat misleading admission as a weighting error. The round-5 receipt verified that round-4 text. The verification is not a round-3 closure.
- **P2-05.** ADD §5.5 and §8. Persistence of edge transitions, progress revisions, and outcome records is MUST, fail-closed. Effective state is the latest superseding record. ADD open question 8 is closed.
- **P3-01.** WP §6.1 is the cooldown contract. Former §10.1 references in WP §4.2, §6, and the v0.1.3 changelog row now point at §6.1.
