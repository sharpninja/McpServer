# P0-A Hostile Review: Entire Active Program

Review time: 2026-09-06T18:55:00Z

OverallVerdict: DISAGREE

Class: project implementation planning. This is a bounded review, not implementation approval or a replacement plan. Seven numbered findings must be resolved before P0-A AGREE. No implementation, test execution, deployment, TODO mutation, requirement mutation, or commit is claimed.

## Exact Review Target

- The assistant's user-approved `<proposed_plan>` headed `BDPv4 Completion Plan: Entire Active Program`, not the older persisted plan.
- The exact block was extracted from the inherited assistant message in the reviewer's native transcript. UTF-8 SHA-256, including the two proposed-plan tags: `5E3E9D2C1F9AFED1D5234177958F696D582FAC0EFA6FE2720C2898F1E8039A2A`; 11,842 characters.
- Native reviewer: Codex, delegated thread `01a07803-5454-7123-a1ee-e4304a59a588`, nickname Fermat. Transcript `C:\Users\kingd\.codex\sessions\2026\09\06\rollout-2026-09-06T13-37-59-01a07803-5454-7123-a1ee-e4304a59a588.jsonl` has turn_context model `gpt-6-astra`, effort `xhigh`, turn `01a07803-615e-79d1-b98d-8794cdc527c2`. This verifies the requested model, not an inferred label.
- Commands used only PowerShell.Mcp 1.14.0 `execute_command`, isolated console #22008, `agent_id=sa-7186f21d`. Manual receipt editing used `apply_patch`.
- Read the complete 154-line AGENTS.md, complete 397-line AGENTS-README-FIRST.yaml, complete 115-line BDPv4 document, workspace Copilot instructions, add-profile skill, all 18 eligible global profile files, and all 3 non-profile linked memory files. All wiki links resolved. Old linked REST/cache guidance was not used to override current plugin rules.
- Parent and preservation-agent baseline inventories were deliberately not duplicated. No parent session/cache files were edited. No historical worker was restarted.

## Live Contract Evidence

Through installed Codex plugin `C:\Users\kingd\.codex\plugins\cache\mcpserver-codex-plugin\mcpserver\1.106.0\Invoke-CodexMcpPlugin.ps1`:

- `-Command Invoke -Method workflow.todo.get -ParamsObject @{id='PLAN-PLUGINHANDOFF-001'}` returned the existing locked plan. Its description is 773 string-array lines; SHA-256 of those lines joined with LF is `5DDBA69E4FA227277F134A075573058D76944E924DF436E811D973D6042740C8`. References below to locked TODO lines use that numbered array, not an on-disk generated projection.
- `-Command Invoke -Method workflow.requirements.effective -ParamsObject @{}` returned active `layer-1`, order 1: 333 functional, 457 technical, 487 testing records and 333 FR mapping rows. The inspected HOSTILEREVIEW, HYGIENE, WIKIEXPORT-003..005, and PLUGININT families have explicit FR/TR/TEST mappings. This is not a missing-requirements claim.
- Plugin manifest itself reports version 1.106.0. Status reports available with this reviewer's distinct cache, hasSession true and hasCurrentTurn true. Native transcript metadata, not that status label, proves model/effort.

## Findings

### 1. P1: The replacement plan drops executable per-slice gates and the requirements-to-RED review boundary

Approved plan anchors: Execution and Gates; Requirements and Validation; Ordered Implementation 1-13.

Conflict: marker lines 311-315 require a decision-complete handoff naming the unit tests to add/update first, expected RED, GREEN criteria, and exact validation scope for each Byrd gate. Locked TODO lines 70-82 and 84-86 require those details and a hostile review after requirements, before tests encoding new behavior; lines 280-282 name that Phase A exit. The new plan names project families and global target names, but no per-increment test methods or executable scope manifest. Its repeating sequence starts with an AC-to-test plan and reaches hostile RED only after tests; the post-requirements hostile checkpoint is not explicit. Existing TEST records name historical tests, but do not assign the new worker/import/security deltas to bounded gates.

Minimum correction: add a decision-complete gate ledger to the approved text and persisted TODO before P0-B. For each bounded increment, name its FR/TR/TEST/AC, test methods, mock expectation, real RED assertion, exact commands/configuration/platform/provider, discovery inventory, prior scope, and GREEN exit. Explicitly place hostile AGREE after requirement changes and before RED code. Retain existing named tests without pretending they are new REDs. Include the recovered provider inventory and real Pester/aiUnit/native Linux commands, not just Nuke target names. Test-ledger creation is a P0 planning artifact, not deferred implementation discretion.

### 2. P1: The internal review worker lacks a complete ownership and failure contract

Approved plan anchor: Ordered Implementation 9, particularly atomic claims, renewable leases, fenced completion, bounded retries, cancellation, and existing agent infrastructure.

Conflict: marker 313 requires failure/data/interface decisions before implementation. Effective FR/TR-MCP-HOSTILEREVIEW-003 and TEST-MCP-HOSTILEREVIEW-003 cover execution identity, not the newly selected autonomous worker lifecycle. The plan does not specify its persisted states and transitions, claim/agent-job correlation, lease/renewal/timeout bounds, retry eligibility and limits, queue capacity/admission behavior, or what cancellation means while keeping exactly four public operations. Existing AgentPoolOptions exposes MaxQueueSize=200 (`src/McpServer.Services/Options/AgentPoolOptions.cs:19`); AgentPoolService creates its own job ID, checks capacity, and records queued state (`src/McpServer.Services/Services/AgentPoolService.cs:278`). Saying to reuse the pool does not resolve the crash boundary between a durable review claim, pool admission, and result commit.

Minimum correction: lock one worker state machine and its configuration/default bounds, reuse or explicitly map existing pool policies, define attempt versus execution identity and durable job linkage, and define restart/takeover behavior at every enqueue/result-persistence boundary. Specify internal-only cancellation versus a permitted existing cancellation surface without inventing a fifth public review operation. Add structured AC and named tests for admission rejection, worker restart, lease loss, stale completion, duplicate delivery, exhausted retry, cancellation, and model-unavailable behavior. Preserve the requested Astra/xhigh model and read-only review contract.

### 3. P1: Review-artifact secrecy and resource bounds are not carried through the one-shot pipeline

Approved plan anchor: Ordered Implementation 9. The 1 MiB submit bound constrains the request containing links, not the resolved documents or generated result.

Conflict: FR-MCP-HOSTILEREVIEW-002 requires supported, authorized artifact resolution with diagnostics; the marker requires secret protection and no silent omissions. Effective FR-MCP-SESSIONLOGSAN-001 AC1-3 requires outbound session-log redaction while stored originals remain unchanged. The selected reuse path calls OneShotSensitivePromptPolicy.Publish (`src/McpServer.Services/Services/AgentPoolService.cs:303`) and exposes the published prompt in notifications at line 327. The inspected policy only redacts HandoffTodoDraft (`src/McpServer.Services/Services/OneShotSensitivePromptPolicy.cs:17`). No existing protection for a new hostile-review context was demonstrated. The plan does not choose a protected context or policy for linked session content, worker notifications/persisted jobs, findings that echo secrets, resolved-content limits, or stale authorization at dispatch. These are design gaps, not a claim that an unimplemented worker already leaked data.

Minimum correction: specify the one-shot review context and protected publication policy; resolve artifacts through authorized sanitized read surfaces; revalidate scope at execution; bound individual/aggregate resolved content and model output; record truncation/refusal explicitly. Define which raw bytes may enter model execution and prohibit their retention in queue metadata, notifications, logs, and persisted results. Name tests with secret sentinels in linked sessions and model output, oversized resolved artifacts, revoked access, and cross-workspace links. Do not expand this into an unrelated sanitizer rewrite.

### 4. P1: Atomic hydration and replay are not reconciled with the actual workspace-create boundary

Approved plan anchors: Ordered Implementation 11-12, `--dump` on workspace creation, transactional remapping, idempotent replay, and no partial hydration.

Conflict: FR-MCP-WIKIEXPORT-004 AC3 forbids partial writes on invalid input; TR-MCP-WIKIEXPORT-004 AC4 requires idempotent reimport. The mandated create route presently calls WorkspaceService.CreateAsync, then InitAsync, then starts the workspace (`src/McpServer.Support.Mcp/Controllers/WorkspaceController.cs:125`). CreateDatabaseAsync rejects an existing active workspace at line 317 and saves the workspace, writes its projection, and publishes a change at lines 337-340 (`src/McpServer.Services/Services/WorkspaceService.cs`). These are observable side effects outside an unspecified later row-import transaction. Normal replay also encounters the existing-workspace conflict before any unspecified import receipt lookup.

Minimum correction: define the import coordinator and atomic boundary for registration plus hydration, including receipt lookup before duplicate-create rejection, the supported storage/provider path, and when projections, init, marker generation, startup, and change notifications may occur. Distinguish precommit rejection/rollback from postcommit activation failure with an explicit recoverable receipt; do not imply filesystem/process work is covered by an EF transaction. Define concurrent identical and conflicting import behavior, restore/tombstone policy, and cancellation points. Name tests proving no externally active partial workspace, repeatable postcommit recovery, and no overwrite of pre-existing filesystem data.

### 5. P1: A policy-registry requirement does not yet define portable treatment of executable and machine-owned rows

Approved plan anchors: Ordered Implementation 11-12, table-policy registry, dependency closure, remapped machine keys, preserved shared-record skip-if-exists behavior.

Conflict: the locked TODO's table list at line 603 includes FederationProxies, FederationOperations, FederationOutbox, HandoffIngestionRuns, TriageResearchRuns, AgentDefinitions and other machine/runtime records. Generic containment, FK remapping, and skip-if-exists do not say which of these may become live on the new server. FederationOperationEntity retains replayable method/path/headers/body and defaults to queued (`src/McpServer.Storage/Entities/FederationOperationEntity.cs:38`). GetSyncItemsAsync selects unacknowledged imported outbox rows and joins their operations (`src/McpServer.Services/Services/FederationTopologyService.cs:581`). Thus row import can carry actionable work, not just passive history. The new plan leaves per-table/column policies, resumed-job eligibility, source credentials, and destination-owned identity authoritative behavior to implementation.

Minimum correction: approve the actual table/column portability matrix before importer RED tests, covering operational ownership, dependency closure, identities, source-only history, shared collisions, secret fields, tombstones, and queued/in-progress work. Keep imported historical operations/jobs inert unless a separately authorized replay protocol explicitly permits them; do not silently run source-host commands after hydration. Bind matrix coverage to the EF model, include omissions as diagnostics, and define how required-but-nonportable rows differ from malformed missing tables. Add startup/restart tests showing that import cannot trigger source-host outbox, agent, triage or Handoff work. Preserve approved migration history; use additive provider migrations only where the chosen design requires them.

### 6. P2: Historical provenance preservation conflicts with the unqualified old-ID-absence acceptance criterion

Approved plan anchor: Ordered Implementation 12 says to preserve historical provenance as history rather than rewriting its meaning.

Conflict: effective FR-MCP-WIKIEXPORT-004 AC2 says, exactly, `Old workspace id does not appear in the new store.` TR-MCP-WIKIEXPORT-004 AC2 repeats old-ID absence; TEST-MCP-WIKIEXPORT-004 requires `Import_RemapsWorkspaceIdAndPaths_OldIdAbsent`. The locked TODO import contract also remaps workspace paths. Retaining an old workspace ID inside imported audit/provenance data makes that unqualified test fail; globally replacing historical text would instead falsify provenance. The approved plan does not explicitly amend or disambiguate this AC.

Minimum correction: expressly reconcile the AC before code. Define exactly which operational identity/FK/path columns must contain only destination identity and where original identity may remain as immutable, non-operative provenance. Update the corresponding FR/TR/TEST through MCP under the approved amendment, and add tests for both scopes. Do not achieve absence by rewriting historical narrative or weaken the operational isolation test.

### 7. P2: Portable dump confidentiality is not an executable projection contract

Approved plan anchor: Ordered Implementation 11 excludes live credentials while exporting workspace-scoped rows and dependency closure.

Conflict: locked TODO line 603 includes session/audit rows and the replay headers/body described in finding 5. Effective FR-MCP-SESSIONLOGSAN-001 explicitly says stored entities remain unchanged and redaction is outbound-only; therefore exporting database rows is not equivalent to exporting already-sanitized query DTOs. The plan does not bind its credential exclusion to a column/embedded-payload policy or specify detection failure handling, and `live credentials` leaves historical credentials and secrets embedded in arbitrary session/audit text ambiguous. FR-MCP-WIKIEXPORT-003 AC3 simultaneously requires TODO rows/links to match the store, so redaction versus fidelity must be explicitly scoped rather than improvised.

Minimum correction: specify an export-only redaction/omission policy covering scalar and nested/encoded sensitive payloads, diagnostic behavior without secret echoes, and which semantic fields must remain lossless. Apply hashes to the final exported bytes and verify the same projection on import without modifying original stored logs. Lock failing tests using credentials in session dialog, audit JSON, and replay headers/body, plus unchanged requirement/TODO relationship fidelity. Reuse the existing sanitizer where applicable; no unrelated sanitizer project is required.

## Scope That Is Sound

- The new code/review model assignment is explicit and verified for this reviewer. Historical BUG-139 work must not be relabeled as new Sol/Astra output. Parent supplied the candidate trailer `GPT-5@Codex` with no effort; truthful historical attribution still needs evidence under marker 309. This is not permission to fabricate a replacement trailer.
- Preservation comes before integration, prohibits discarding unrelated dirty develop work, and protects shared remotes from force pushes. The other agent owns baseline inventory; this receipt does not independently attest to the reported 396 dirty entries or present branch state.
- BUG-139 is pinned to base `210cb223d81dac6b4045b868e4d7b2712d08b752`, candidate `808ec049d56daf214c391beccd6b9d69bb9867c6`. The inspected remediation report lines 6, 10-15 and 29-33 supports treating it as locally implemented but not independently approved. Native Linux/FUSE, full final regression, Nuke-capacity, and integrated-candidate gates remain real unfinished work. No focused-result substitution is permitted by the plan.
- Parent supplied the recovered historical provider TRX path `C:\Users\kingd\AppData\Local\Temp\BUG-TRIAGE-139-twenty-second-20260905T231500Z\final\commit-78cdb9f9-exact-final\provider-manifest\twenty-second-final-provider-manifest.trx`, SHA-256 `E00969C662678107F45E2C0EC2683A7D284BE1055D895952F89674E4BCD168C3`, 107/107/0/0 across 27 classes. This is attributed parent evidence, not an independent rerun or a missing target. Its exact inventory belongs in finding 1's gate ledger.
- The plugin and aiUnit corrections are justified by inspected code: PluginSessionLogWorkflowAdapter launches Status then performs lifecycle calls directly through the client (`tests/McpServer.PluginIntegration.Tests/PluginSessionLogWorkflowAdapter.cs:67` and `:85`); AiStrategyFixture parses JSON locally (`tests/McpServer.PluginIntegration.Tests/AiStrategyFixture.cs:20`), and the AiTheory fabricates a completed receipt (`tests/McpServer.PluginIntegration.Tests/PluginSessionLogAiTheoryTests.cs:30`). The plan requires real plugin lifecycle execution, server proof, real aiUnit, and deterministic assertions that AI cannot override. No new implementation finding is invented here.
- Keep-open behavior is correct: MCP-WORKSPACEHYGIENE-002 and the umbrella remain open without further owner authorization. A truthful hygiene mismatch finding for an intentionally held item must not be hidden or used to auto-close it. Read-only analysis, bounded threshold, and Director exit semantics are preserved.
- Existing migration history is preserved; all three provider gates, additive changes when necessary, Nuke deployment, development validation, separate staging/production approval, and origin/GitHub SHA verification are retained. This bounded review authorizes none of those actions.

## Session Log Evidence

- Supported user-prompt-submit generated session `Codex-20260906T184446Z-plugin-session` and turn `req-20260906T184447Z-prompt-4915`.
- Distinct supported CacheRoot: `F:\GitHub\McpServer\.mcpServer\codex\reviews\01a07803-5454-7123-a1ee-e4304a59a588`. CODEX_THREAD_ID, CODEX_SESSION_ID, and MCP_AGENT_SESSION_ID use the real delegated thread; transcript header points to that same native transcript. No handcrafted MCP session ID was used.
- Installed wrapper Status reported available, hasSession true, hasCurrentTurn true, pendingCount 2, failsafeCount 2. Those counters are shared V4 workspace-pending inventory, not proof that this review failed or persisted. No drain/replay of another session's pending files was requested.
- `client.SessionLog.QueryAsync` through the installed wrapper, then exact local filtering by generated session ID, proved the review turn, interpretation, two review actions, one decision-dialog item and native agentSessionId persisted. QueryTitle is `P0-A hostile plan review`. A text-filter query returned no match; a DateTimeOffset scalar filter was rejected; the supported query without those filters succeeded. Neither of those preliminary attempts was treated as persistence proof.
- The workflow builder currently emits model `codex` and ignores model/designDecisions fields supplied to updateTurn. Actual Astra/xhigh identity is explicitly in the persisted interpretation/tags and is independently verified in the native transcript. Closing evidence and supported metadata correction are recorded below after final verification; no clean-completion claim is made from a silent wrapper exit alone.

## Completion Verification

Pending final wrapper completion and server query at receipt creation. P0-A remains DISAGREE regardless of logging success. The reviewer stops after the bounded receipt and logging closeout.
