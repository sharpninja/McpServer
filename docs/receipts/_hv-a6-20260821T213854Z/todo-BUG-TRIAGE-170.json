{"id":"BUG-TRIAGE-170","title":"Failsafe drain hardcodes a 2s SubmitAsync timeout and aborts the queue","section":"Backlog","priority":"high","done":false,"estimate":null,"note":null,"description":["workflow.failsafe.drain sets ReplFailsafeDraining, then Get-ReplMethodTimeoutSeconds in plugins/core/lib-ps/repl-invoke.ps1 returns a hardcoded 2 seconds for client.SessionLog.SubmitAsync. Invoke-ReplRawCore kills the REPL after that budget and returns 'mcpserver-repl timed out after 2s'. Test-ReplFailsafeBackendUnreachable treats 'timed out' as backend unreachable, so the pass aborts after one record and leaves drainAttempts at 0. The same payload submitted outside drain with REPL_TIMEOUT=90 completed in 73s. The 2s cap was meant to keep a nested drain from blocking getFr/getTr (TR-MCP-PERSIST-003), but it also applies to the operator drain verb and to deferred automatic drain, so live SessionLog failsafe records cannot replay.","Triage group: triage-group-1cf130328c87c15c","Reports: 1"],"technicalDetails":["Acceptance criteria:","- workflow.failsafe.drain replaying client.SessionLog.SubmitAsync must use a timeout at least as large as the non-drain SubmitAsync budget (REPL_TIMEOUT, or a dedicated drain timeout), not a hardcoded 2 seconds.","- A failsafe SessionLog record whose SubmitAsync completes within the configured drain timeout must be replayed and removed from the live queue, not aborted with 'backend unreachable: mcpserver-repl timed out after 2s'.","- An explicit workflow.failsafe.drain of a payload that succeeds outside drain (observed ~73s with REPL_TIMEOUT=90) must not classify that latency as backend unreachable.","- Automatic/nested drain must still avoid blocking an in-flight getFr/getTr caller for a long SubmitAsync (preserve TR-MCP-PERSIST-003 via deferral or an equivalent non-blocking path, not a 2s timeout that cannot succeed).","- A timeout abort during drain must leave drainAttempts unchanged so a later successful pass can still replay the record.","Implementation notes:","- The smoking gun is Get-ReplMethodTimeoutSeconds: if ($script:ReplFailsafeDraining -and $Method -eq 'client.SessionLog.SubmitAsync') { return 2 }. Both Invoke-WorkflowFailsafeDrain and Invoke-ReplFailsafeDrainOnFirstSuccessCore share Invoke-ReplFailsafeDrain, so the cap hits operator drain and automatic drain.","- Do not reuse the 2s cap as the nested-drain safety valve. ReplRawInFlight / ReplFailsafeDrainDeferred already defer drain so it does not sit inside getFr/getTr. Raise or remove the drain-specific SubmitAsync timeout and keep that deferral.","- Default non-drain timeout is still 30s unless REPL_TIMEOUT is set. Observed successful submit was 73s, so drain should use REPL_TIMEOUT, REPL_HELPER_TIMEOUT, or a dedicated REPL_FAILSAFE_DRAIN_TIMEOUT large enough for real session-log payloads.","- Test-ReplFailsafeBackendUnreachable matching 'timed out' is correct for a true transport failure; the bug is the artificial 2s budget producing that timeout on a reachable backend."],"implementationTasks":null,"completedDate":null,"doneSummary":null,"remaining":null,"priorityNote":null,"reference":null,"phase":null,"dependsOn":null,"functionalRequirements":["FR-MCP-TRIAGE-002","FR-MCP-172"],"technicalRequirements":["TR-MCP-TRIAGE-004","TR-MCP-PERSIST-003","TR-MCP-REPL-012"],"idempotencyKey":null}
