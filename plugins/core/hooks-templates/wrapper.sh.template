#!/usr/bin/env bash
# __HOOK_NAME__.sh - generated McpServer plugin hook wrapper (__HOST__).
# Generated from plugins/core/hooks-templates; do not edit in the plugin repo.
# All logic lives in lib/hook-lib.sh; host knobs live in lib/plugin-env.sh.
set -uo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$SCRIPT_DIR/__DEPTH__/lib/plugin-env.sh"
. "$SCRIPT_DIR/__DEPTH__/lib/hook-lib.sh"
hook_env_init __CACHE_MODE__
__ENTRY__ "$@"
